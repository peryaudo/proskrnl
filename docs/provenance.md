# Provenance

Per-component record of where code and constants come from (docs/11,
CONTRIBUTING "Provenance rules"). Kernel-code reference material is limited to
**Wine headers and official Microsoft documentation** — never ReactOS, never
leaked Windows source, never model memory.

| Component | Provenance |
|---|---|
| `abi/ntstatus.h`, `abi/ntdef.h`, `abi/ntobapi.h`, `abi/ntmmapi.h`, `abi/ntimage.h`, `abi/ntpsapi.h` | **Generated** by `tools/gen_abi.py` from `third_party/wine/include/{ntstatus.h,ntdef.h,winnt.h,winternl.h,ddk/wdm.h}` (Art. 4) — including the M3–M5 `Nt*` prototypes, the section enums/structs, and the PE/COFF on-disk structures, extracted verbatim. Never hand-edited; regenerate with `make gen-abi`. |
| `kernel/mm/section.c`, `kernel/mm/fault.c` | `Nt*` signatures via the generated `abi/ntmmapi.h`; boundary behaviour (creation/mapping statuses, guard-page semantics) pinned by `tests/ntapi/sem_mm/` against the Wine oracle, with Wine's `server/mapping.c` + `dlls/ntdll/unix/virtual.c` as the behaviour reference (behaviour, not code). Internals (section body, page-cache seed, VAD view plumbing) original. |
| `kernel/mm/pecoff.c` | PE/COFF structures from the generated `abi/ntimage.h` (Wine's `winnt.h` — the layout Microsoft's public PE/COFF specification documents); relocation-block walk per the documented `IMAGE_REL_BASED_*` semantics (Wine's `LdrProcessRelocationBlock` reimplementation as behaviour reference). Parser/mapper implementation original. |
| `kernel/init/initrd.c` | Original (the M5 seed RAM-disk; internal shape free per Art. 1 — nothing here is user-observable). |
| `abi/ntioapi.h` | **Generated** by `tools/gen_abi.py` from `third_party/wine/include/{winnt.h,winternl.h}` (Art. 4): `IO_STATUS_BLOCK`, `PIO_APC_ROUTINE`, `FILE_INFORMATION_CLASS`, the `FILE_*_INFORMATION` structs, file access/share/attribute flags, create options/dispositions, and the M6 `Nt*` prototypes, extracted verbatim. |
| `kernel/io/` | `Nt*` signatures via the generated `abi/ntioapi.h`; `IoCheckShareAccess`/`IoSetShareAccess`/`IoRemoveShareAccess` names per NT's documented exports (signatures adapted to the internal `IO_FCB`); boundary behaviour pinned by `tests/ntapi/sem_file/` against the Wine oracle (`dlls/ntdll/unix/file.c` + `server/fd.c` + `server/async.c` as behaviour reference, not code — `async_close_obj_handle`'s handle-close cancel rule is transcribed as *observable semantics*, cited at the site). Internals (FILE_OBJECT, vfs table, completion path) original — no IRP (docs/03). |
| `fs/fat32/` | On-disk format written from the **Microsoft FAT Specification** (Aug 30 2005, fatgen103 successor) and **UEFI Specification 2.10 §5** (GPT), cited per-constant in comments (G8). NT semantics layer pinned by `tests/ntapi/sem_file/`. No Linux/GRUB/mtools FAT code consulted; implementation original. |
| `drivers/pci.c`, `drivers/virtio/` | Written from the **PCI Local Bus Specification 3.0** (config mechanism #1, Type 00h header) and the **OASIS virtio 1.2 cs01 specification** (device status, split virtqueue, modern PCI capabilities, the virtio-blk protocol §5.2, the virtio-input event queue and wire format §5.8, and the virtio-snd control/tx protocol §5.14 — AUD-1), cited per-constant in comments (G8); the pinned `third_party/qemu` is the runtime cross-check (for snd: `include/standard-headers/linux/virtio_snd.h`, a header of constants, and the `hw/audio/virtio-snd.c` model's behaviour). No Linux driver code consulted; implementation original. |
| `drivers/fb.c`, `drivers/fbproto.h` (GUI-1) | Written from the **Limine boot protocol** (`third_party/limine-protocol/include/limine.h` + `PROTOCOL.md`, pinned submodule, 0BSD) for the framebuffer response layout, and **official Microsoft documentation** ("Defining I/O Control Codes") for the custom ioctl function-code range; `FILE_DEVICE_VIDEO` is generated into `abi/ntioapi.h` from the pinned Wine `winioctl.h` (Art. 4). No display-driver source of any kind consulted; implementation original. |
| `drivers/hid.c`, `drivers/hidproto.h` (GUI-1) | The read contract is the virtio-input wire format passed through verbatim (see the `drivers/virtio/` row); no keyboard layout or scancode table is transcribed from anywhere, because the driver deliberately performs no translation. `FILE_DEVICE_KEYBOARD` is generated (Art. 4). Implementation original. |
| `drivers/snd.c`, `drivers/sndproto.h`, `tests/audio/` (AUD-1) | The ioctl contract relays the virtio-snd PCM structures verbatim (see the `drivers/virtio/` row); no format conversion, resampling, or mixing code exists to have a source, because the driver deliberately performs none. The custom ioctl function-code range is **official Microsoft documentation** ("Defining I/O Control Codes"); `FILE_DEVICE_SOUND` is generated into `abi/ntioapi.h` from the pinned Wine `winioctl.h` (Art. 4). The host-side WAV checker parses the RIFF container from its public layout (Python stdlib `struct`). No audio-driver source of any kind consulted; implementation original. |
| `kernel/mm/pagecache.c` | Original (the unified page cache, docs/05: "page cache = (file, offset) → physical page"). |
| `kernel/ob/` | `Ob*` export signatures and `OBJECT_HANDLE_INFORMATION`/`MODE` from `third_party/wine/include/ddk/wdm.h`; `Nt*` signatures via the generated `abi/ntobapi.h`; NTSTATUS conventions pinned by `tests/ntapi/sem_ob/` against the Wine oracle. `OBJECT_TYPE.closeProcedure`'s shape (the process plus the two handle counts) is **official Microsoft documentation**, the *ObjectCloseMethod routine* page, and the per-process handle count is `server/handle.c` `get_obj_handle_count` as a behaviour reference, not code. Internals (object header, handle table, namespace walk) original (docs/03: internals ours). |
| `kernel/se/` (CUI-2) | `Nt*` signatures and every numeric constant via the generated `abi/ntseapi.h` (Wine's `winnt.h`/`winternl.h`); boundary behaviour pinned by `tests/ntapi/sem_se/` against the Wine oracle, with `dlls/ntdll/unix/security.c` + `server/token.c`/`server/handle.c`/`server/object.c` as the behaviour reference (behaviour, not code — the fixed identity's composition, the access-check algorithm's step order and the buffer protocols are transcribed as *observable semantics*, each with a G8 citation). Internals (single-block TOKEN layout, SEP SD blob, capture/marshalling) original. |
| `kernel/lib/rtl.c` | `Rtl*` signatures from `third_party/wine/include/winternl.h`; implementations original. The upcase fold is the generated table in the row below; the three-level lookup expression itself is the one the NLS file's format dictates (`dlls/ntdll/locale.c` `casemap`, `server/unicode.c` `to_lower` — behaviour, not code: it is how that data structure is indexed, and there is no other way to index it). |
| `kernel/lib/upcase.h` | **Generated** by `tools/gen_upcase.py` from `third_party/wine/nls/l_intl.nls` (the pinned tree's NLS casemap file) — 1433 `USHORT` deltas, extracted verbatim, never hand-edited; regenerate with `make gen-nls`, and `make` re-runs `--check` so it cannot drift from the pin. **This is `abi/`'s arrangement, not Flanterm's**: no third-party *code* is linked into the kernel — numeric contract values are mechanically extracted from the pinned Wine tree and compiled in, exactly as `abi/ntstatus.h`'s values are extracted from Wine's LGPL headers under Article 4. It is here rather than in `abi/` because the source is NLS data rather than a header. The values themselves are Wine's own machine output from the Unicode Character Database (`third_party/wine/tools/make_unicode`), and they are the contract: name equality at the boundary is decided by this table, so a rule that approximated it was a divergence (docs/03 "Name case folding"). |
| `kernel/cm/license.h` | **Generated** by `tools/gen_license.py` from `third_party/wine/loader/wine.inf`'s `[LicenseInformation]` section — the eleven name/type/value rows that write `HKLM\Software\Wine\LicenseInformation` into a wineprefix, extracted verbatim, never hand-edited; regenerate with `make gen-license`, and `make` re-runs `--check` so it cannot drift from the pin. **`abi/`'s arrangement, the `kernel/lib/upcase.h` row above**: no third-party *code* is linked into the kernel — this is contract DATA mechanically extracted from the pinned Wine tree and compiled in, under Article 4. It is here rather than in `abi/` because the source is INF data rather than a header. It is the contract in the same sense the upcase table is: `NtQueryLicenseValue` names that key literally (`dlls/ntdll/unix/registry.c`), so on the oracle leg the answer IS this payload, and a kernel that seeded a plausible subset of it diverged (docs/03 M10 winetest notes; docs/21 W12). |
| `kernel/cm/timezones.h` | **Generated** by `tools/gen_timezones.py` from `third_party/wine/dlls/kernelbase/kernelbase.rgs` — the 139 time-zone key rows (2576 values, including the 92 `Dynamic DST` per-year subkeys) that the WINE_REGISTRY resource inside the pinned kernelbase writes into a wineprefix at `wineboot --init` (`dlls/setupapi/fakedll.c` `register_resource`), extracted verbatim, never hand-edited; regenerate with `make gen-timezones`, and `make` re-runs `--check` so it cannot drift from the pin. **`abi/`'s arrangement, the two rows above**: no third-party *code* is linked into the kernel — this is contract DATA mechanically extracted from the pinned Wine tree and compiled in, under Article 4. It is the contract in the same sense the license payload is: `GetTimeZoneInformationForYear` (`dlls/kernelbase/locale.c`) resolves a caller-named zone out of that key, so on the oracle leg the answer IS this table, and a kernel that seeded one row of it diverged (docs/03; docs/21 W13). |
| `kernel/mm/kasan.c` | Original; implements the public ASan compiler ABI (`__asan_*` outline hooks, 1/8 shadow encoding) as documented by LLVM/the sanitizer interface headers. Not derived from the Linux kernel's KASAN sources. |
| `kernel/ke/` dispatcher types (`DISPATCHER_HEADER`, `KEVENT`, `KSEMAPHORE`, `KMUTANT`, `KTIMER`, `KWAIT_BLOCK`, `KWAIT_REASON`) | Struct shapes and `Ke*` function signatures taken from `third_party/wine/include/ddk/wdm.h` and `dlls/ntoskrnl.exe/sync.c` (headers/signatures only — Wine's implementation is a user-space emulation and was not translated). Member casing per docs/15. Internal layouts (`KTHREAD`, scheduler, wait algorithm) are original (docs/03: internals ours). |
| `kernel/lib/list.h` | NT list API names/signatures as declared in Wine's `wdm.h`; implementation original (the semantics are fully specified by the declarations). |
| `kernel/mm/` (phys, pool), `kernel/ke/` scheduler/wait internals | Original, written to Art. 3 (one lock, one pool, no preemption). |
| `arch/x86_64/` (IDT, LAPIC/xAPIC, PIT calibration, paging, context switch, SMBIOS, fw_cfg) | Original, from public hardware documentation (Intel SDM, OSDev-common PIT/LAPIC facts, DMTF DSP0134 for SMBIOS), the Limine boot protocol (`third_party/limine-protocol/include/limine.h`, pinned submodule, 0BSD), and — for `fwcfg.c`, the QEMU firmware-configuration device — the pinned QEMU tree's own **specification document** `third_party/qemu/docs/specs/fw_cfg.rst`. That is a spec read, not a code read: no QEMU source was translated, and the device is emulated hardware described publicly, in the same position virtio's spec occupies for `drivers/virtio/`. |
| `third_party/flanterm` (submodule; the boot console) | Pinned submodule of the official upstream (`mintsuki/flanterm`, BSD-2-Clause), used **unmodified** — no local edits, no vendored copy, no build-time patching. Its two C files are compiled by our own Makefile and **linked into the kernel image**: the first third-party code for which that is true (lwIP became the second — its row below), permitted because BSD-2-Clause is GPL-2.0-compatible (docs/11). `kernel/init/bootvid.c` calls only the public `flanterm.h` / `flanterm_backends/fb.h` API; nothing of Flanterm's is translated, reimplemented, or read as a reference for our own code. |
| `third_party/lwip` (submodule; the TCP/IP protocol engine) | Pinned submodule of the official upstream (`git.savannah.nongnu.org/git/lwip.git`, fetched via the project's own GitHub mirror `github.com/lwip-tcpip/lwip` — same history, same pin; the mirror is the .gitmodules URL because Savannah outages were taking every CI shard down with them, tag `STABLE-2_2_1_RELEASE`, BSD-3-Clause — text re-verified at the pin: `COPYING`), used **unmodified** — no local edits, no vendored copy, no build-time patching; the submodule carries zero local commits. Its core sources are compiled by our own Makefile and **linked into the kernel image**: the second third-party code for which that is true (after Flanterm), admitted under docs/11's four conditions, argued in docs/24 §3. We call its **public API only** (`netif_add`, `pbuf_*`, the raw `tcp_*`/`udp_*` callback API, `dhcp_start`, `sys_check_timeouts`); its internals are never reference material for our own code — the AFD layer above it is written against the *oracle* (docs/24 §1b). The port layer (`drivers/net/port/`: `lwipopts.h`, `arch/cc.h`, the `sys_now` shim) is lwIP's documented user-supplied configuration surface, not a patch. |
| `kernel/init/bootvid.c` | Original. The framebuffer geometry comes from the **Limine boot protocol** (`third_party/limine-protocol/include/limine.h` + `PROTOCOL.md`, pinned submodule, 0BSD) — the same response `drivers/fb.c` publishes; the glyph rendering is Flanterm's (row above), called as a library. NT's boot-time equivalent (bootvid.sys / `InbvDisplayString`) was not consulted and is not reproduced: nothing here is observable from an unprivileged `.exe` (Art. 1). |
| `third_party/limine`, `third_party/limine-protocol`, `third_party/qemu` | Pinned submodules of the official upstreams (limine-bootloader on GitHub; QEMU's official GitHub mirror). Build/run tooling only — none of their code enters the kernel image; Limine's BIOS stages live on the boot disk under their own BSD license. |
| `tests/` | Original; harness style modelled on Wine's test conventions (`ok()`, snake_case), no Wine test code copied yet. |
| `third_party/wine` (submodule; the M7 userland) | Pinned fork of winehq (`proskrnl-target` branch, LGPL-2.1+). The **built PE binaries** (`ntdll.dll`, `kernel32.dll`, `kernelbase.dll`, and on the winetest image the resource-only `tzres.dll` the time-zone table's `MUI_*` values name) and the **NLS data files** (`nls/*.nls`) are baked onto the boot disk image at build time — distributed under Wine's LGPL, and no Wine **code** is compiled into or linked with the kernel (the process boundary is what keeps that true, docs/11). What DOES enter the kernel image, and only ever mechanically, is generated numeric contract data: `abi/*` from Wine's headers (Art. 4), `kernel/lib/upcase.h` from `nls/l_intl.nls` and `kernel/cm/timezones.h` from `dlls/kernelbase/kernelbase.rgs` (rows above). Fork commits touch only the unixlib seam (Art. 10 / G9); the diff vs. the winehq merge-base is the hack meter. |
| `tests/clients/` | Original (the M7/M9 acceptance clients). `hello.c`: MS-ABI PE linked against the pinned Wine ntdll import library; the hand-written `.seh_` frame mirrors the shape of Wine's own `process_breakpoint` (structure, not code). `m9_*.c`: threaded blocking pipes + console round trips through kernel32/kernelbase. |
| `abi/ntcondrv.h` | **Generated** by `tools/gen_abi.py` from `third_party/wine/include/{wine/condrv.h,wincontypes.h,basetsd.h}` (Art. 4): the `IOCTL_CONDRV_*` surface, its parameter structs, and the console-inheritance sentinels, extracted verbatim. |
| `fs/npfs/`, `drivers/condrv.c` | `Nt*` signatures via the generated `abi/ntioapi.h`/`abi/ntcondrv.h`; pipe boundary behaviour pinned by `tests/ntapi/sem_pipe/` against the Wine oracle (`server/named_pipe.c` as behaviour reference, not code); the kernel⇄conhost transport mirrors wineserver's `get_next_console_request` **semantics** (`server/console.c` — behaviour, not code; the wire format `drivers/condrvproto.h` is original and proskrnl-internal). Implementations original. |
| `user/wine/programs/conhost/` | Original glue only (`proskrnl_glue.c`: entry, mini-CRT, headless user32/window stands-ins). **Wine's conhost is NOT copied**: the Makefile compiles `third_party/wine/programs/conhost/{conhost.c,proskrnl.c}` straight from the pinned tree, where the wineserver seam lives as a runtime-dormant fork commit on `proskrnl-target` (Art. 10 — behind the no-wineserver probe; LGPL-2.1+, user-mode only, never compiled into or linked with the kernel, the same license boundary as the baked Wine DLLs, docs/11). |
| `user/wine/programs/cmd/`, `tests/cui/` | Original glue only (`proskrnl_glue.c`: the CRT entry plus the five user32 / four shell32 stand-ins, written against MS documentation with Wine named as behaviour reference). **Wine's cmd is NOT copied**: the Makefile links `third_party/wine/programs/cmd/x86_64-windows/*.o` + `cmd.res`, produced unmodified by the pinned tree's own PE build (LGPL-2.1+, user-mode only, never compiled into or linked with the kernel — the same license boundary as conhost and the baked Wine DLLs, docs/11). `tests/cui/` is original test code. |
| `user/wine/programs/rundll32/`, `user/wine/programs/wineboot/` | Original glue only (`proskrnl_glue.c`: the CRT entry plus headless stand-ins for the user32/gdi32 wait-window set and the shell32/shlwapi/ws2_32/wininet/newdev legs, written against MS documentation with Wine named as behaviour reference). **Wine's rundll32/wineboot are NOT copied**: the Makefile links `third_party/wine/programs/rundll32/x86_64-windows/rundll32.o` and mingw-compiles `programs/wineboot/wineboot.c` straight from the pinned tree (LGPL-2.1+, user-mode only, never compiled into or linked with the kernel — the same license boundary as conhost/cmd, docs/11). |
| `user/smss/` | Original (the session manager: the one image the kernel launches; `launch.c` spawns everything else through `NtCreateUserProcess` — console children get ConDrv handles opened by the device paths the pinned kernelbase itself uses, named as behaviour reference — and `session.c` drives the acceptance flows/test sweeps; `firstboot.c` is the CUI-1 hand-off spawning `wineboot.exe --init`). |
| `tools/filter_inf.py` | Original (strips file/registration/ini/profile directives from `wine.inf` at image-bake time, keeping the AddReg payload). |
| `tools/gen_sysini.py`, `tools/gen_license.py`, `tools/gen_timezones.py` | Original extractors over pinned Wine **data**. The first two read `wine.inf`: the first emits the `%windir%\{win,system}.ini` bytes from `[SystemIni]` at image-bake time, the second emits `kernel/cm/license.h` from `[LicenseInformation]` (row above). Both read the INF as **data** and copy no Wine code; the setupapi `FLG_ADDREG_*` flag words `gen_license.py` matches on are cited to `third_party/wine/include/setupapi.h` at their definition (G8). `gen_timezones.py` is the same shape over `dlls/kernelbase/kernelbase.rgs`: it implements ATL's registrar script grammar as the pinned tree's own `dlls/atl/registrar.c` (`get_word`, `do_process_key`) defines it — behaviour, not code, and cited there — and emits `kernel/cm/timezones.h` (row above). |
| `LICENSE` | Verbatim GPL-2.0 license text fetched from the canonical FSF source (`https://www.gnu.org/licenses/old-licenses/gpl-2.0.txt`), not model-recalled. |
| dosfstools (`fsck.fat`) | Host-side **test oracle only** (`tests/run/fatcheck.sh`), installed by `tools/setup_linux.sh`/`Brewfile`. GPL-3.0+; never linked into or translated into the kernel — the same standing as QEMU and sgdisk (docs/11). |
| `tests/run/fatsweep.py` | Original host-side FAT32 invariant checker, written from the "Microsoft FAT Specification" (Aug 30 2005) with sections cited inline — the same public-spec provenance as `fs/fat32/`. |
| `third_party/freetype` (submodule; GUI-2, GUI-3) | Pinned upstream FreeType at `VER-2-13-3` (FreeType Project License / GPL-2.0 dual). Built twice from one file list by `tools/build_freetype.sh`: cross-built as a PE static library linked into `win32u.dll`, and (GUI-3) built native as `libfreetype.so.6` for the **host-side oracle only** -- the pinned Wine is configured `--with-freetype` against that library so the font-metrics oracle and the target's font backend are the same FreeType, the same host-oracle standing as QEMU and dosfstools (docs/11). Both outputs are **user mode / host side only**, never compiled into or linked with the kernel image, the same license boundary as the baked Wine DLLs (docs/11). No FreeType code is copied or translated; the file list is the one `docs/INSTALL.ANY` specifies for a manual build. No external dependencies are enabled (no system zlib/libpng/harfbuzz/brotli). |
| `user/wine/dlls/win32u/`, `user/wine/wineserver-lite/`, `user/wine/include/` (GUI-2) | Original glue only, with one declared exception: `wineserver-lite/server/rawinput.c` carries a verbatim copy of the kbdus scancode->vkey tables (`third_party/wine/dlls/win32u/input.c` `vsc_to_vk`/`_e0`/`_e1`, macro-identical via `include/kbd.h`) and mirrors the `KEYEVENTF_SCANCODE` resolution of `dlls/win32u/message.c`, both cited at the copy -- LGPL-2.1+ user-mode code alongside the rest of the Wine-derived userland, never near the kernel, re-verified on a pin bump. Its `server/inputmap.h` (evdev-keycode-to-scancode, values as in `dlls/winewayland.drv/wayland_keyboard.c` `key2scan` -- AT set-1 scancodes fixed by the keyboard's own contract -- named as the behaviour reference; the `\Device\Input0/1` call sequence is `drivers/hidproto.h`, proskrnl's own) has the same standing. Otherwise **win32u and wineserver are NOT copied**: the Makefile compiles `third_party/wine/dlls/win32u/*.c` and eleven `third_party/wine/server/*.c` straight from the pinned tree (LGPL-2.1+, user-mode only, never linked with the kernel -- the same boundary as conhost/cmd). The glue is the environment those sources expect: pthreads over `Rtl*`, the user-mode callback pair, ntdll's unix-side helpers, the POSIX file calls the font backend makes, and the client half of `wine_server_call`. `hash_strW`/`memicmp_strW` re-express `server/unicode.c`'s two functions over ntdll's case table (behaviour, not code -- the file itself is a unix-fd reader and is not compiled); the minimal directory object is built out of `server/object.c`'s own namespace engine. Export lists and the request dispatch table are **generated** from the pinned tree (`tools/gen_win32u_def.py`, `tools/gen_server_table.py`, `tools/gen_freetype_syms.py`), never typed (Art. 4). |
| `user/wine/dlls/winefb.drv/` (GUI-2) | Original, except the cursor decode. Implements Wine's `struct user_driver_funcs` as declared in `third_party/wine/include/wine/gdi_driver.h`; `dlls/winewayland.drv` is named as the *behaviour* reference for the surface-create/flush shape. `cursor.c`'s HCURSOR-to-pixels decode is **copied** from the pinned tree's own drivers (LGPL Wine code into an LGPL Wine driver, `docs/11`): the monochrome AND/XOR decode from `dlls/winewayland.drv/wayland_pointer.c` (`create_mono_cursor_buffer`, itself adapted from wineandroid.drv) and the colour path — 32bpp DIB, has-alpha probe, "more than 10% alpha" cut, AND-mask fallback — from `dlls/winex11.drv/mouse.c` (`create_xlib_color_cursor`); the hotspot centring is both drivers'. The `\Device\Fb0` call sequence is `drivers/fbproto.h`, which is proskrnl's own. (The `\Device\Input0/1` readers and the evdev-keycode-to-scancode table live in `wineserver-lite/server/` — see that row.) |
| `user/wine/dlls/winevsnd.drv/` (AUD-2) | Original. Implements mmdevapi's unixlib surface as declared in `third_party/wine/dlls/mmdevapi/unixlib.h` (the 37-entry `enum unix_funcs` contract) as PE code over `Nt*` calls; `dlls/winealsa.drv/alsa.c` and `dlls/winepulse.drv/pulse.c` are named as the *behaviour* references for the ring/clock arithmetic and the WASAPI error shapes (the winetest render pairs pin exactly those), never copied. The `\Device\Snd*` call sequence is `drivers/sndproto.h`, which is proskrnl's own (HACK-007). |
| `drivers/nsi.c` | Original. Serves the `IOCTL_NSIPROXY_WINE_*` dialect as declared in `third_party/wine/include/wine/nsi.h` via the generated `abi/nsi.h` (Art. 4); the wire semantics (count probe, capacity-spaced arrays, the refusal statuses) are pinned by `tests/ntapi/sem_nsi/` against the oracle measured from OUTSIDE — `dlls/nsiproxy.sys` is never read (docs/24 §1b). Rows are views of lwIP netif state through its public API only. |
| `user/wine/dlls/wsresolv/` (Net-3) | Original. Implements ws2_32's five-entry resolver unixlib as declared in `third_party/wine/dlls/ws2_32/ws2_32_private.h` (the param structs + `enum ws_unix_funcs` contract) as PE code over `Nt*` and ws2_32's own public socket API — the winevsnd.drv shape. The buffer-packing contracts are read from the PE consumer (`dlls/ws2_32/protocol.c`, behaviour reference); `dlls/ws2_32/unixlib.c` (the unix-side implementation) is never translated. The DNS client (`dns.c`) is written from RFC 1035/3596 — public spec, cited inline — and convicted by the hermetic `tests/resolv` corpus. |
| `third_party/curlwin` (curl-for-win; the Net-3 acceptance tool) | Prebuilt third-party **binary**, downloaded sha256-pinned by `tools/setup_linux.sh` from curl.se (curl 8.21.0_7 win64-mingw, LibreSSL statically linked; curl license — `COPYING.txt` retained beside the binary). Used **unmodified as a guest test payload only**: baked onto the test image and run as an ordinary process — never linked with, translated into, or read as reference for the kernel or any proskrnl code (the point of the leg is that the tool is off-the-shelf; docs/02 "Net-3"). Not distributed by the repository (gitignored; the purchase re-runs in setup). |
| `tools/gen_apiset_forwarders.py` | Original. Reads a PE's own import table (objdump) and emits forwarder DLLs whose exports forward to ucrtbase — the api-set names and symbol sets come from the binary being provisioned, nothing recalled; the forwarder-export mechanism is standard PE (`Forwarder RVA`). |
