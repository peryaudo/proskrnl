# The M10 stretch gate (docs/02 "Ideal regression: the CUI subset of Wine's
# own test suite"): every <test_exe>:<subtest> pair here must exit 0 (zero
# winetest failures) under the pinned Wine oracle AND on proskrnl
# (tests/run/run.sh winetest). Curated: a pair joins only once it is green on
# BOTH runners; pairs blocked on unimplemented surface or oracle-side
# failures stay out with their causes recorded in docs/03 "M10 winetest
# notes".
ntdll_test.exe:atom
ntdll_test.exe:env
ntdll_test.exe:error
ntdll_test.exe:large_int
ntdll_test.exe:rtlbitmap
ntdll_test.exe:rtlstr
ntdll_test.exe:string
# ntdll_test.exe:time is out: oracle-side USD/NtQuerySystemTime race on
# shared CI runners (docs/03 "M10 winetest notes")
kernel32_test.exe:atom
kernel32_test.exe:codepage
kernel32_test.exe:directory
kernel32_test.exe:environ
kernel32_test.exe:format_msg
kernel32_test.exe:heap
msvcrt_test.exe:data
msvcrt_test.exe:dir
msvcrt_test.exe:environ
msvcrt_test.exe:heap
msvcrt_test.exe:printf
msvcrt_test.exe:scanf
msvcrt_test.exe:signal
msvcrt_test.exe:string
ucrtbase_test.exe:environ
ucrtbase_test.exe:printf
ucrtbase_test.exe:scanf
ucrtbase_test.exe:string
# CUI-5 unpark: cmd's directory subtests joined once rename landed (its
# 6 proskrnl-only failures were all the missing MoveFile path). The
# ntdll/kernel32 change pairs stay parked on the single-record/no-buffering
# watch model - causes in docs/03 "M10 winetest notes".
cmd.exe_test.exe:directory
# CUI-6 added no pairs: no parked pair above was blocked on the handle/
# identity/query surface it built, and none was added unverified (a pair
# joins only once green on BOTH runners). The object-manager/token subtests
# are candidates for a future unpark once a winetest run confirms them.
# CUI-7 added no pairs either: the natural candidates (ntdll:virtual,
# kernel32:virtual - parked since M10 on the missing Mm surface) both
# exercise placeholder allocation (test_NtAllocateVirtualMemoryEx and the
# VirtualAlloc2 blocks), which proskrnl deliberately refuses loudly
# (docs/03 "CUI-7"; no baked consumer) - and under the armed
# panic-on-NOT_IMPLEMENTED boot that refusal is fatal by design, so the
# pairs cannot run to a verdict until placeholders are built.
# CUI-8 added no pairs, as docs/19 par.8.5 predicted for a milestone that
# adds no Nt* - and the par.8.5 re-check for the invisible case (a pair
# parked on overlapped-I/O BEHAVIOUR rather than on a missing id) found
# none: every parked pair above names a missing surface (placeholders, the
# watch-buffering model, oracle-side races), not an async-file-data
# behaviour. The ring-3 answers CUI-8 pinned (the async-handle pending
# shape) were already the oracle's, so no pair's verdict could have moved.
