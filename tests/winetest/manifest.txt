# The winetest gate's manifest (tests/run/run.sh winetest): the FULL non-GUI
# sweep of Wine's own test suite — every subtest of ntdll, kernel32,
# programs/cmd, msvcrt and ucrtbase, run under the pinned Wine oracle AND on
# proskrnl from ONE binary per module (Makefile `wtests`, the pinned tree's
# own unmodified test objects linked standalone).
#
# Syntax: `<exe>:<subtest>[:<timeout_s>]`, one per line. A line whose first
# character is '#' is a COMMENT, and so are blank lines — both readers agree
# (tests/run/run.sh winetest, and user/smss/session.c
# SessionParseWtestManifest, which is what the kernel-side sweep parses off
# the baked image). Comments are whole-line only: there is no trailing-#
# form, because a '#' is a legal character in neither field and adding one
# would make a typo'd pair name parse as a shorter pair rather than fail
# loudly. The optional third field is the per-pair timeout in seconds
# (default 300).
#
# advapi32 is deliberately out: its subject is the security/registry service
# surface, covered by the CUI-2/CUI-3 legs (tests/run/run.sh console, scm) and
# by tests/ntapi, not by this gate. user32 is out too — the GUI trophy has its
# own manifest (manifest-gui.txt, tests/run/run.sh guiwtest).
#
# COVERAGE IS THE COMMENTS, THE GATE IS THE ACTIVE LIST. This file lists
# every pair the five modules have; the ACTIVE (uncommented) lines are the
# ones the gate runs and must keep green, and every pair that is not green
# today is commented out with its triage written above it. Nothing is
# deleted — a commented-out pair is a worked frontier entry, not a dropped
# one, and its block says how many assertions still fail and why.
#
# ---------------------------------------------------------------------------
# WHY A PAIR IS COMMENTED OUT
#
# Two different reasons, and each block below says which it is.
#
#   1. RED AND FIXABLE. The pair fails on proskrnl for a reason that is a
#      kernel gap, the block names the cause and the current failure count,
#      and it ends with `# TODO: Implement`. It is commented out so the gate
#      stays a pass/fail signal rather than a permanently-red wall; the
#      TODO is the work item, and the counts are the yardstick for the next
#      person to check progress against.
#
#   2. NOT REACHABLE UNDER THE SCOPE. The pair cannot go green without
#      changing something the project has decided not to change, so it
#      carries NO TODO. These blocks name the deciding constraint (the FAT32
#      backend, the CUI/GUI split, a constitution article) so the exclusion
#      can be re-argued if the constraint ever moves.
#
# The older, narrower categories below still apply and still name their
# measurement — they are the subset of (2) that predates this sweep:
#
#   (a) proskrnl will never implement the surface, by a decision already
#       recorded elsewhere (docs/03, an ADR). The pair cannot go green
#       without reversing that decision, so leaving it in would make the
#       gate permanently red on purpose.
#
#   (b) the ORACLE cannot serve as the spec for it — it fails or dies on the
#       pinned Wine itself, or it asserts something about the standalone
#       link rather than about the boundary. A pair that is red with zero
#       proskrnl involvement cannot convict the kernel (Art. 6), so it
#       cannot sit in a differential gate.
#
#   (c) the pair's subject is only reachable on the GUI image — it needs a
#       DLL whose imports pull in user32/gdi32, so running it here would
#       mean putting the GUI stack on the CUI volume (G7). This is the same
#       reason user32 is out of this file; such a pair belongs in
#       manifest-gui.txt when that image can carry it, not here. The
#       exclusion must name the specific dependency and show that the
#       failures downstream of it convict nothing — a pair that ALSO fails
#       for a kernel reason stays in and stays red.
#
# Every oracle-side measurement below was taken as an ORDINARY USER under a
# UTF-8 locale, which the runner now enforces (tests/run/run.sh): before that
# it ran as root under C/POSIX, and eight pairs looked oracle-red that were
# not — ntdll:directory (82 failures), ntdll:file (6), ntdll:change (1),
# kernel32:profile (4), kernel32:version (36), kernel32:comm (3),
# kernel32:file (7) and ucrtbase:file (a crash) are all 0-failure green once
# the host stopped leaking into the answer. docs/03 "M10 winetest notes" has
# the whole story; do not re-park a pair on an oracle failure without
# re-measuring it that way first.
#
# ONE OF THOSE EIGHT GOES RED ON SOME DEVELOPER HOSTS, AND IT IS NOT THE
# KERNEL — kernel32:version, now PARKED for that reason (its block below has
# the full signature and the trade). It fails the ORACLE leg on at least one
# KVM developer box with exactly the 36 failures the root/C-POSIX era
# produced, while proskrnl's half passes and CI is green on both legs.
#
# It used to stay ACTIVE on the strength of being green on CI. That was the
# wrong trade: it made `make fulltest` permanently red on a developer box,
# and a suite that is always red is a suite people stop reading — the same
# argument this header makes two paragraphs up for never activating a red
# pair. Coverage lost to a parked pair is visible in this file; attention
# lost to a red gate is not.
#
# The lesson for the NEXT host-local residue: park it, write the signature
# down, and say what would un-park it. Do not leave the gate red and expect
# everyone to remember why.
# ---------------------------------------------------------------------------

# --- ntdll (third_party/wine/dlls/ntdll/tests) ------------------------------
# (a) ALPC is permanently unimplemented: proskrnl's local-RPC transport is
#     npfs, so the whole LPC/ALPC syscall surface stays unbuilt on purpose
#     (docs/03 "CUI-3 SCM notes"). Green on the oracle, and proskrnl panics
#     on the first NtAlpcCreatePort — which is the armed-panic contract
#     working, not a defect to chase.
# ntdll_test.exe:alpc
ntdll_test.exe:atom
ntdll_test.exe:change
# AT ITS FAT FLOOR — NOT FIXABLE WHILE THE BACKEND IS FAT32.
#     5 failures out of 6393 tests, from dead at the start of this session.
#     All five are ONE cause: directory.c:1446 fails
#     FSCTL_SET_REPARSE_POINT with STATUS_INVALID_DEVICE_REQUEST, so the file
#     never becomes a reparse point — and the four EaSize assertions at
#     :1568-:1593 are reading the reparse TAG, because EaSize aliases
#     ReparsePointTag in the directory classes for a file that has one.
#     Reparse points are an NTFS construct. FAT32 has nowhere to put one, and
#     inventing an on-disk extension would be writing a filesystem the public
#     spec does not describe (docs/11). Re-open only if the backend changes.
# ntdll_test.exe:directory
ntdll_test.exe:env
ntdll_test.exe:error
# TWO SEPARATE BLOCKERS, BOTH MEASURED, NEITHER A WOW64 ONE. Re-measured at
#     the WOW64 milestone, which changed this entry's story: the oracle used
#     to be RED with 25 failures and then die at 0xc0000005 without printing
#     a summary, and 23 of those needed a 32-bit subsystem the oracle build
#     did not have. It has one now (WINE_CONFIGURE gained i386), so
#     test_debug_registers_wow64 (exception.c:9299-:9393) and
#     test_wow64_context (:4589-:4637) get their syswow64 children, the
#     fatal crash in that tail is gone, and the ORACLE now runs to a summary
#     with FOUR failures:
#
#       - exception.c:6357/:6361 RegisterClassA/CreateWindowA -> 0x78, and
#         :6198/:6204 behind them (KiUserCallbackDispatcher never found,
#         because the window it needs was never created).
#
#     THAT DIAGNOSIS BLAMED THE WRONG THING, and the fix for what it blamed
#     is what proved it. This block used to read "-> 0x78 under --without-x
#     (`nodrv_CreateWindow` in the log)" and concluded "the remaining oracle
#     gap is DISPLAY-ONLY, and re-opening that half is a decision about the
#     ORACLE BUILD (give the leg a display)". The oracle now HAS a display
#     (--with-x on the runner's Xvfb, tools/setup_linux.sh + tests/run/run.sh
#     start_xvfb) and the four did not move. Measured on that oracle, same
#     display, same prefix:
#
#       - the PINNED TREE's own ntdll_test.exe (which imports the real
#         user32) runs the whole module GREEN: 5915 tests executed, 65
#         marked as todo, 0 failures, exit 0;
#       - the CUI gate's own standalone binary (Makefile `wtests`,
#         build/wtests/ntdll_test.exe) still answers 4 -- the same four --
#         at 5395 tests executed.
#
#     The difference between the two binaries is tests/winetest/glue/
#     user32_stubs.c: the standalone link stands user32 in with stubs that
#     return ERROR_CALL_NOT_IMPLEMENTED (0x78) BY DESIGN, because user32 is
#     the GUI path and is constitutionally off the CUI image (Art. 7). The
#     0x78 was ours all along; nodrv would have produced the same number,
#     which is how it got read as nodrv's.
#
#     So the remaining oracle gap is NOT a decision about the oracle build --
#     it is the CUI gate's own stub policy, working exactly as its header
#     says it should ("a subtest whose assertions need a real window/
#     winstation fails IDENTICALLY under the oracle and on proskrnl and stays
#     off the manifest"). Re-opening this half would mean putting a real
#     user32 on the CUI image, which Art. 7 answers no. The pair stays parked
#     on the proskrnl-side defects below, which are the real work item.
#
#     The proskrnl side is now the larger half, and it is a REAL defect with
#     nothing to do with WOW64 — recorded here so it is not mistaken for
#     oracle noise again:
#
#       - exception.c:3316/:3318 report the faulting address and RIP one
#         byte PAST the instruction for an NX (instruction-fetch) fault;
#       - exception.c:3409-:3435 read back the wrong `fs` in a captured
#         CONTEXT, and :3420 answers 0x9d (STATUS_UNSUCCESSFUL) from
#         NtGetContextThread;
#       - the leg then dies at RIP=0 on a #PF with err=0x14 (user,
#         instruction fetch, no-execute).
#
#     Each is its own work item against `kernel/ps/usermode.c`, and each is
#     convictable now that the oracle prints a summary — which is the actual
#     change this re-measurement bought. Until they are fixed the pair stays
#     parked, but no longer for the reason it used to say.
#
#     The old triage block here blamed NtSetContextThread ContextFlags
#     selectivity for a RIP=0 kill and was never measured against a green
#     oracle; selectivity IS implemented (kernel/ps/usermode.c
#     KiTrapFrameToContext / KiContextToTrapFrame, the docs/review-2026-07
#     §9 fix). docs/21 W6 has that correction.
# ntdll_test.exe:exception
# GATED ON A WHOLE SUBSYSTEM, FIXABLE. 36 failures, then a panic at the
#     missing NtCreateMailslotFile — so, like kernel32:mailslot, it is
#     blocked on mailslots as a device rather than on the file surface it is
#     named for. The 36 are real and countable now: file.c:5266-:5431 is
#     NtReadFile answering STATUS_END_OF_FILE where success was wanted (a
#     cluster of its own), :4979-:4983 wants 0xc0000022 and gets success,
#     and :467 is FILE_OPEN_BY_FILE_ID, which is also kernel32:volume's last
#     item.
# TODO: Implement
# ntdll_test.exe:file
ntdll_test.exe:generated
# BOUNDED BY A DECISION ALREADY TAKEN — NOT FIXABLE BY KERNEL WORK.
#     Fifteen system-information classes were built this session and both
#     entry points now owe nothing the oracle serves. The pair still panics,
#     at SystemFirmwareTableInformation (76), which IS implemented: it
#     refuses STATUS_NOT_IMPLEMENTED for a provider other than RSMB, and the
#     armed boot turns that into a panic.
#     The oracle refuses identically, and that does not resolve it. G12 says
#     NOT_IMPLEMENTED is never pinned, because an oracle answering it is
#     unbuilt rather than authoritative. But info.c:1623 asserts exactly that
#     status, with no broken() guard — so here it is measured WINDOWS
#     behaviour, and G12's premise ("nobody depends on this status") is false
#     for this one call.
#     Implementing ACPI/FIRM does not reach it: the assertion is about
#     provider ZERO, which is not a provider.
#     DECIDED (docs/21 §3): this pair is BOUNDED and G12 is NOT amended.
#     The KiPinnedNotImplemented escape hatch would buy one pair and spend
#     the one property that makes G12 work — that it has no door. Do not
#     re-open this on the strength of this call alone; a SECOND call found
#     to pin NOT_IMPLEMENTED as Windows behaviour would be new evidence.
# ntdll_test.exe:info
ntdll_test.exe:large_int
# (b) The oracle CRASHES here (exit 5, no summary line): om.c:1893 wants
#     \Sessions\1\Windows\WindowStations\WinSta0 and NtQueryObject answers
#     c0000008, because the winstation half of the object namespace is the
#     user32/GUI path that is off this image by Art. 7. Fails without
#     proskrnl involvement, so it cannot convict the kernel.
# ntdll_test.exe:om
ntdll_test.exe:path
# PARTLY FIXED, FIXABLE — the 19 npfs failures are GONE and the pair now
#     stops one item further on, which is progress with a new frontier.
#     What they were: five refusals npfs did not owe anybody an answer for,
#     all five now implemented and pinned by
#     tests/ntapi/sem_pipe/create_refusals.c (docs/21 W11) —
#       :344 x12  FSCTL_PIPE_LISTEN on the CLIENT end is
#                 STATUS_ILLEGAL_FUNCTION, not INVALID_PARAMETER;
#       :357 x4   the pipe's SHARE mask bounds the CLIENT's access (a
#                 FILE_SHARE_READ pipe is OUTBOUND: readable, never
#                 writable) — the inverse of the usual share-mode reading;
#       :281      create with sharing 0 is INVALID_PARAMETER, because a pipe
#                 must NAME a direction;
#       :291      FilePipeLocalInformation needs FILE_READ_ATTRIBUTES on the
#                 handle;
#       :296      FILE_CREATE on an existing pipe is ACCESS_DENIED, not the
#                 OBJECT_NAME_COLLISION a file create gives.
#     That took it to ZERO failed assertions and a PANIC at NtFsControlFile
#     with a user ApcRoutine. W4a (the ioctl APC leg) is now BUILT too
#     (kernel/io/{ioctl,async}.c, pinned tests/ntapi/sem_pipe/listen_apc.c),
#     and the pair runs THROUGH test_overlapped into the completion tests.
#
#     Those two revealed one failed assertion (pipe.c:413) and a panic at
#     class 41, and BOTH ARE NOW FIXED as well:
#       - pipe.c:413 — an ioctl refused ON THE SPOT still leaves the caller's
#         completion event CLEAR, with the IOSB untouched. kernel/io/ioctl.c
#         now routes that through IopAbandonRequest, the same authority the
#         transfer paths use.
#       - class 41, FileIoCompletionNotificationInformation, is built in both
#         directions (kernel/io/query.c; the word lives on the FILE_OBJECT).
#         The VERB only: the bits accumulate, unknown bits are masked, a
#         synchronous handle is refused — and NONE of the three changes
#         behaviour yet, which docs/16 states plainly. Pinned by
#         tests/ntapi/sem_pipe/ioctl_event.c.
#
#     SO THE PAIR NO LONGER PANICS AT ALL. It ran the completion tests and
#     ended with FIVE failures, which were ONE subject — THE COMPLETION-
#     PACKET RULES — and are now fixed (see below):
#       - :1539 x2, after FSCTL_PIPE_PEEK on a port-bound handle, where a
#         packet is owed ("NtRemoveIoCompletion returned 102" — a timeout —
#         and the value left at its 0xdeadbeef poison). kernel/io/rw.c's
#         IopCompleteTransfer is the one site that posts, and ioctl.c calls
#         IopCompleteRequest directly instead, so no ioctl ever produces a
#         packet. The rule to reuse is the same one rw.c already has:
#         completionValue = apc ? 0 : apcContext, posted only when non-zero.
#       - :1548 and :1567, where a packet was posted and none was owed
#         because the handle carries FILE_SKIP_COMPLETION_PORT_ON_SUCCESS.
#       - :1561, a STATUS_BUFFER_OVERFLOW read on a handle carrying
#         FILE_SKIP_COMPLETION_PORT_ON_SUCCESS still queues a packet
#         (GetQueuedCompletionStatus returned 234 = ERROR_MORE_DATA, so one
#         WAS dequeued) — because the skip is not implemented at all. When it
#         is, note the axis: the oracle skips when the call did NOT return
#         STATUS_PENDING (dlls/ntdll/unix/file.c's `ret_status ==
#         STATUS_PENDING` argument to add_completion, server/fd.c
#         add_fd_completion's `req->async ||`, server/async.c's
#         `async->pending ||`), and its guard is !NT_ERROR, which lets
#         BUFFER_OVERFLOW through to the skip. NOT on the completion status:
#         a first cut of this session keyed it on NT_SUCCESS inside
#         IopCompleteTransfer, which runs BEFORE IopAsyncReturnShape turns
#         success into STATUS_PENDING — so an async port-bound handle with
#         the flag set would have answered PENDING and posted nothing,
#         hanging GetQueuedCompletionStatus forever. The decision has to be
#         made where the RETURNED status is known.
#     CORRECTION, and it matters for how the five are read: this pair does
#     NOT reach its summary line. It ends `FAIL (timeout)` — five failed
#     assertions and then a WEDGE, reproducibly. An earlier revision of this
#     block claimed "the first measurement that is not a lower bound"; that
#     was wrong, and it was wrong because the count was read without reading
#     the verdict line beside it. The five are still a lower bound.
#
#     THE FIVE ARE FIXED. ioctl.c's inline completion now runs through
#     rw.c's IopCompleteTransfer — so an ioctl posts a completion packet
#     exactly as a transfer does — and FILE_SKIP_COMPLETION_PORT_ON_SUCCESS
#     is honoured on the correct axis. Measured: the five go to ZERO failed
#     assertions. Pinned by tests/ntapi/sem_pipe/completion_packet.c, green
#     on both runners.
#     THE AXIS is the part to get right and it is NOT the completion status:
#     the oracle skips only when the call did not REPORT STATUS_PENDING to
#     its caller (add_completion's `ret_status == STATUS_PENDING`,
#     add_fd_completion's `req->async ||`, async_set_result's
#     `async->pending ||`), and its guard is !NT_ERROR, so
#     STATUS_BUFFER_OVERFLOW is skipped too. In proskrnl the caller passes
#     that answer down (IopWillReportPending), because the DEVICE paths
#     return the status unchanged while the DISK paths run it through
#     IopAsyncReturnShape afterwards. Keyed on NT_SUCCESS instead, an async
#     port-bound handle would answer STATUS_PENDING and post nothing —
#     GetQueuedCompletionStatus hangs forever.
#     THE PAIR'S VERDICT DOES NOT MOVE, and that is the honest bit: zero
#     failed assertions and then the same wedge, so it still reads FAIL
#     (timeout). The five being gone is a real measurement; the pair being
#     sound is not something this can show.
#
#     THE PENDED PACKET IS NOW BUILT TOO, and it was NOT the wedge — the
#     prediction this block used to carry was half right and the half that
#     mattered was wrong. IopCompletePendingRequest (kernel/io/async.c) really
#     did post no packet at all, so an overlapped caller that bound a port and
#     waited on a pended ioctl waited forever; it now posts through the same
#     rw.c site the inline tail uses (IopPostRequestPacket), the
#     IOP_PENDING_REQUEST holds a REFERENCED file object, and the port is read
#     at COMPLETION rather than captured at issue because the oracle re-reads
#     (measured: binding a port after a listen has pended still posts).
#     Pinned by tests/ntapi/sem_pipe/pending_packet.c, green on both runners.
#     But the pair does not move: still ZERO failed assertions, still
#     FAIL (timeout), wedging in the same place.
#
#     THE WEDGE IS GONE. It was npfs data reads never pending: on an
#     asynchronous handle NpfsRead parked the calling thread in NpfsWait
#     instead of answering STATUS_PENDING, and pipe.c:1578 issues exactly that
#     read on an EMPTY pipe through an overlapped handle and then satisfies it
#     from the SAME thread at :1583 — so the thread parked inside the read and
#     the write it was waiting for was never issued. Built (docs/21 W4c,
#     docs/03 "CUI-8 async notes"): the read parks an IOP_PENDING_REQUEST on
#     the pipe END and the peer's write completes it, through the engine's new
#     buffer/length legs rather than a fourth bookkeeping shape. Pinned by
#     tests/ntapi/sem_pipe/pended_read.c.
#     Two more rules came with it, both convicted by this pair and both pinned
#     in the same file: exactly ONE of the caller's event and the FILE OBJECT
#     is signalled at a PENDED completion, and FILE_SKIP_SET_EVENT_ON_HANDLE
#     FREEZES that state in both directions (:1680-:1702), where docs/16 used
#     to file it as unbuilt.
#
#     SO THE PAIR RUNS 5 MINUTES FURTHER AND IS MEASURED FOR THE FIRST TIME —
#     and that is why the count is 23 rather than 0. The 0 above was a stopped
#     pair's lower bound (docs/21 §4 trap 2), and NONE of the 23 is new: every
#     one is past pipe.c:1578, where nothing had ever executed. It ends in a
#     PANIC now instead of a timeout, so 23 is a lower bound too.
#
#     THE BLOCKING REQUEST'S SIGNALLED STATE IS NOW BUILT: 23 -> 12, and it
#     was the 13 this block predicted, exactly — :1678, :1740 x2, :1742 x4,
#     :1753 x4 and the two todo_wine successes at :1829, all in one step. A
#     request that PARKS ITS CALLER owes the caller's event and the file object
#     what a pended one does; the oracle queues a blocking async through the
#     same code, and `async->blocking = !is_fd_overlapped(fd)` decides only how
#     the CALLER waits (server/async.c create_async). So the event is reset at
#     issue, the handle goes down for the duration, and exactly one of the two
#     goes back up at the end — kernel/io/{async,rw}.c
#     IopBeginBlockingRequest/IopEndBlockingRequest, pinned by
#     tests/ntapi/sem_pipe/blocking_signal.c.
#     :1753 is a blocking FLUSH rather than a read (:1850 and :1877, one per
#     options run) — the same rule through NtFlushBuffersFile, which has no
#     event parameter and can therefore only ever take the file-object arm.
#     :1678 came with it and was NOT a separate item, which is the part worth
#     carrying: it used to be a DELIBERATE cost, because condrv BORROWS the
#     file object as its own readiness signal and a completion-side re-signal
#     spins conhost's poll loop (measured on kernel32:virtual, docs/03). The
#     borrowing is DECLARED now (FILE_OBJECT.deviceManagedSignal, honoured
#     inside the one transition every producer goes through) instead of being
#     paid for by every other device, so the cost is gone rather than traded.
#     Whether anything was hiding behind the 13 is NOT knowable here and this
#     block does not claim it: the pair panics before its summary line, so
#     there is no executed count on either side of the change to compare —
#     only the per-line failure histogram, which lost exactly those 13 lines
#     and gained none. The pair stops in the same place.
#
#     FilePipeInformation's SET LADDER IS NOW BUILT: 12 -> 0, and every one of
#     the twelve was a guard that was missing or in the wrong PLACE. The
#     ladder is split across the two halves of the oracle and that split is
#     what decides which mistake gets reported — LENGTH, then RANGE (both in
#     dlls/ntdll/unix/file.c, above the server call and therefore above the
#     handle), then the HANDLE, then DISCONNECTED, then the byte-pipe rule
#     (server/named_pipe.c set_named_pipe_info). proskrnl had only the last of
#     the five. Landed in kernel/io/query.c + fs/npfs/pipe.c, pinned by
#     tests/ntapi/sem_pipe/pipe_mode_set.c; docs/21 W11, docs/03 "The
#     FilePipeInformation SET ladder".
#     :886/:894/:941 were the SERVER end's FILE_WRITE_ATTRIBUTES (the CLIENT
#     end needs no access at all — an asymmetry that falls out of the
#     handler's two-lookup retry, not out of any rule); :926/:931/:984/:989
#     were the range bound `(CompletionMode | ReadMode) & ~1`; and the five
#     "Unexpected CompletionMode" lines at :888/:896/:907/:918/:936 were
#     CONSEQUENCES of the first — a wrongly-accepted set really changed the
#     server end's mode, so the state the test read back afterwards was wrong
#     for the rest of the function. Read as five separate assertions they look
#     like a query bug; there is none (docs/21 §4 trap 4).
#
#     FileAccessInformation (CLASS 8) IS NOW BUILT and the PANIC IS GONE:
#     test_filepipeinfo runs to its end, and with it the whole of
#     test_pipe_with_data_state — still ZERO failed assertions. The class is
#     one ACCESS_MASK and both halves of it are counter-intuitive: the value
#     is the access of the HANDLE ENTRY (server/fd.c
#     default_fd_get_file_info's get_handle_access, i.e. the caller's own
#     handle-table word — a weakened duplicate reports the weaker mask, and a
#     zero-access duplicate reports zero and still SUCCEEDS), and the class's
#     info_sizes[] entry is 0, so its length check sits BELOW the handle
#     lookup where every table-driven class's sits above it. Landed in
#     kernel/io/query.c (+ IopReferenceFileByHandle gaining Ob's optional
#     OBJECT_HANDLE_INFORMATION out-parameter, so the entry's word comes from
#     the one resolver), pinned by tests/ntapi/sem_file/access_info.c;
#     docs/21 W11, docs/03 "FileAccessInformation (class 8) asks Ob".
#     This block's own estimate of the item was WRONG in the way docs/21 §4
#     trap 4 keeps being wrong: docs/21 called it "a small one — the class
#     reports the granted access of the handle, which FILE_OBJECT.grantedAccess
#     already holds". The first clause held; the second is false, and an
#     implementation that took the advice passes every winetest assertion in
#     this pair and answers the create's mask through every duplicate.
#
#     THE ALERTABLE PARK IS NOW BUILT and the :3187 WEDGE IS GONE: the pair
#     runs FIVE more functions — test_alertable, test_nonalertable,
#     test_cancelio and into test_cancelsynchronousio — and reports 0 -> 1
#     failed assertion. The 1 is docs/21 §4 trap 2's arithmetic and NOT a
#     regression: it sits at pipe.c:725, three functions past where anything
#     had ever executed, and the histogram before :3187 is still empty.
#     The wedge was exactly what the block above declined to record as a
#     finding, and measuring it first was still right: a FSCTL_PIPE_LISTEN
#     parked on a FILE_SYNCHRONOUS_IO_ALERT handle with a user APC already
#     queued. proskrnl folded ALERT into NONALERT below FileModeInformation,
#     so IoWaitCancellable waited non-alertably for both and a listen with no
#     client coming never returned. The oracle keeps them apart in ONE
#     argument, repeated in server_read_file, server_write_file and
#     server_ioctl_file (dlls/ntdll/unix/file.c:5746/:5781/:5823):
#     `wait_async( wait_handle, (options & FILE_SYNCHRONOUS_IO_ALERT) )`.
#     NtFlushBuffersFileEx is the oracle's own exception and passes FALSE
#     (:6876), which the first draft got wrong in the other direction.
#     Landed in kernel/{ke/ke.h,io/{async,ioctl,rw,file}.c} + fs/npfs/pipe.c,
#     pinned by tests/ntapi/sem_pipe/alertable_park.c; docs/21 W11, docs/03
#     "FILE_SYNCHRONOUS_IO_ALERT: the park is alertable".
#     Two findings under it are worth more than the wedge:
#       - an INTERRUPTED request completes NOTHING — no IOSB, no completion
#         routine, no event signal — because the oracle's async is still
#         QUEUED and only the client stopped waiting. It returns the wait's
#         own STATUS_USER_APC, which is an NT_SUCCESS value, so it is carried
#         as an engine FLAG and never read off a device's return (the
#         STATUS_PENDING lesson, docs/21 W4c). Windows CANCELS instead and
#         answers STATUS_CANCELLED; the test marks that todo_wine, so matching
#         NT here would score a failure for being closer to NT.
#       - npfs was a SECOND FILE_OBJECT construction site and had already
#         drifted (Art. 11). NtCreateNamedPipeFile said it built the object
#         "exactly as IopCreateFile does" and set synchronousIo alone, so
#         every pipe handle reported a zero FileModeInformation whatever it
#         was opened with and its syncIoLock was never initialised. Both sites
#         go through IopCaptureCreateOptions now.
#
#     THE :747 WEDGE IS GONE AND THE PAIR IS MEASURED FOR THE FIRST TIME:
#     1 -> 123 failures across 2386 tests executed (33 todo, 0 flaky, 0
#     skipped), against the oracle's 2378/35/0. THE NUMBER WENT UP BECAUSE THE
#     PAIR STOPPED STOPPING — this is docs/21 §4 trap 2's arithmetic, third
#     time in this block: 1 was a lower bound over a prefix that ended three
#     test functions into test_cancelsynchronousio, and every one of the 123 is
#     past it or was already there. The pair now PRINTS ITS SUMMARY LINE, which
#     no revision of this block has ever had, so from here the executed count
#     is comparable; it also runs in ~11 s instead of eating the full 300 s
#     timeout.
#     The wedge was the FILE-OBJECT half of the blocking-request contract,
#     missing on ioctl.c: pipe.c:747 spins `while ((ret =
#     WaitForSingleObject(ctx.pipe, 0)) == WAIT_OBJECT_0) Sleep(1);` waiting
#     for a blocking FSCTL_PIPE_LISTEN to take the pipe HANDLE down, and
#     nothing took it down. Landed in kernel/io/{async,ioctl,rw}.c +
#     kernel/ke/ke.h, pinned by tests/ntapi/sem_pipe/ioctl_signal.c; docs/21
#     W11, docs/03 "An IOCTL clears the handle at its PARK".
#     THE ORACLE REFUTED THE OBVIOUS TRANSCRIPTION, and that is the part worth
#     carrying: rw.c's engine clears the handle at ISSUE and restores it if the
#     device refuses, which is right for a TRANSFER because pipe_end_read
#     queues every request it gets. An IOCTL's verb is decided ABOVE
#     queue_async (server/named_pipe.c pipe_server_ioctl returns
#     STATUS_PIPE_CONNECTED before it; FSCTL_PIPE_PEEK never queues at all), so
#     an inline peek carrying an event leaves the handle UP on the oracle and a
#     clear-at-issue cannot do that. The clear is now made by the ENGINE at the
#     one place a blocking park happens (IoWaitCancellable) for the services
#     that ask for IopClearAtPark — never by a device.
#
#     THE PIPE'S OWN FILE INFORMATION IS NOW BUILT: 123 -> 99, and the whole
#     24 is one cause in npfs plus one constant in kernel/io/query.c. A pipe
#     end has no unix fd, so the ORACLE routes FileStandardInformation and
#     FileNameInformation to the server (server/named_pipe.c
#     pipe_end_get_file_info) where proskrnl was answering them out of a
#     NpfsGetInfo that returned zeros. AllocationSize is the pipe's two quotas
#     added (a CAPACITY, unmoved by traffic) and EndOfFile is THIS end's
#     readable bytes (so the two ends disagree), both now filled through the
#     raw IO_FILE_INFO shape IopFillStandard already reads; the class's
#     FILE_READ_ATTRIBUTES demand is asked by resolving the caller's handle a
#     second time with the bit, because a UNIVERSAL class cannot state a
#     required access that only one device has; and an end the server
#     DISCONNECTED out from under refuses BOTH classes from one statement in
#     NpfsGetInfo, which every class reading this backend's facts comes
#     through. :1987 was separate and one line: FileNameInformation's length
#     floor is sizeof(FILE_NAME_INFORMATION) (8), not offsetof(.., FileName)
#     (4), so 4..7 bytes is INFO_LENGTH_MISMATCH and not BUFFER_OVERFLOW.
#     Landed in fs/npfs/pipe.c + kernel/io/query.c, pinned by
#     tests/ntapi/sem_pipe/pipe_file_info.c and
#     tests/ntapi/sem_file/name_info_length.c; docs/21 W11, docs/03 "A pipe
#     end's own file information" and "FileNameInformation's length floor".
#     The executed count falls 2386 -> 2376 and the todo markers 33 -> 31, and
#     both are exactly this item: the two DISCONNECTED-client calls used to
#     run test_pipe_with_data_state's `if (!status)` block (5 ok()s each, one
#     of them the :2165 todo) against a query that should have refused. A
#     line-by-line diff of the histogram shows only :1987/:2151/:2160/:2163/
#     :2181/:2447 removed and no other line moved.
#
#     FSCTL_PIPE_PEEK'S STATE LADDER AND ITS OVERFLOW AXIS ARE NOW BUILT:
#     99 -> 80, and the whole 19 is one function (fs/npfs/pipe.c NpfsPeek)
#     transcribed from server/named_pipe.c pipe_end_peek. Four rules, three
#     of which read as bugs. The LENGTH floor is decided above the state, so
#     a short buffer on a listening pipe reports the length. CLOSING is NOT
#     BROKEN while anything is still queued — the oracle's discriminator is
#     `if (!list_empty( &pipe_end->message_queue )) break;`, so one handle
#     answers SUCCESS and then STATUS_PIPE_BROKEN across the one read that
#     empties it. A DISCONNECTED end answers `pipe_end->pipe ?
#     INVALID_PIPE_STATE : PIPE_DISCONNECTED`, and DISCONNECT drops the pipe
#     for the CLIENT only — so the disconnecting SERVER complains about its
#     STATE where its client reports a disconnection, which proskrnl had
#     folded into one answer. And the BUFFER_OVERFLOW is keyed on the PIPE's
#     FILE_PIPE_TYPE_MESSAGE, not on the reading end's read mode (the end's
#     mode does not appear in the oracle's function at all) and is about the
#     FIRST MESSAGE rather than about the whole queue: two messages queued
#     with room for both is a SUCCESS carrying one. Landed in fs/npfs/pipe.c,
#     pinned by tests/ntapi/sem_pipe/peek_state.c; docs/21 W11, docs/03 "What
#     FSCTL_PIPE_PEEK answers, by state and by the PIPE's type".
#     The executed count rises 2376 -> 2377 and that +1 is exactly this item,
#     counted per line: test_pipe_state's `if (!status)` block at :2058 ran 8
#     times and now runs 4 (four of its peeks correctly stopped succeeding),
#     while test_pipe_with_data_state's `if (status == BUFFER_OVERFLOW)`
#     block at :2208 ran 0 times and now runs 5. Todo markers are 31 before
#     and after, and a line-by-line histogram diff shows only :1197/:1199/
#     :2055 removed and :2205 dropping 10 -> 1.
#
#     FSCTL_PIPE_TRANSCEIVE IS NOW BUILT: 80 -> 68, and all twelve are one
#     function (fs/npfs/pipe.c NpfsTransceive) transcribed from
#     server/named_pipe.c pipe_end_transceive. Eleven of the twelve are
#     REFUSALS and the verb's own round trip is the other one: the state
#     question is `if (!pipe_end->connection)` answering
#     `pipe_end->pipe ? INVALID_PIPE_STATE : PIPE_DISCONNECTED`, i.e.
#     FSCTL_PIPE_PEEK's split asked through the same end->orphaned, so
#     LISTENING, CLOSING and a DISCONNECTED *server* all report the STATE and
#     only the client a server threw out reports the disconnection. Then
#     STATUS_INVALID_READ_MODE for an end not in MESSAGE read mode (a mode of
#     the END, which FilePipeInformation moves — not the pipe's type), then
#     STATUS_PIPE_BUSY for a reader that already has buffered bytes, which is
#     TOTAL: the input is not written either. The write half NEVER blocks
#     (the oracle's own "transaction never blocks on write, so just queue a
#     message without async"), so it is NpfsAppendBuffer and not NpfsWrite;
#     the read half is the end's ordinary read machinery through one shared
#     NpfsAwaitRead, minus exactly one rung — pipe_end_read's
#     NONBLOCKING arm has no counterpart here, so a NOWAIT-mode end refuses a
#     read with PIPE_EMPTY and PENDS a transceive in the same state. That
#     pended arm is what gave kernel/io/ioctl.c its CUI-8 data legs (an ioctl
#     that parks still owes its caller a reply). Landed in fs/npfs/pipe.c +
#     kernel/io/ioctl.c, pinned by tests/ntapi/sem_pipe/transceive.c; docs/21
#     W11, docs/03 "What FSCTL_PIPE_TRANSCEIVE writes, and what it refuses".
#     The executed count is unchanged at 2377 and the todo markers at 31, and
#     a line-by-line histogram diff shows only :1450/:1456/:1457/:1458 and
#     :2069x8 removed with no other line moved — nothing was hiding behind
#     the twelve.
#
#     A COMPLETION PORT AND A COMPLETION APC ROUTINE ARE NOW MUTUALLY
#     EXCLUSIVE: 68 -> 60, the eight at :3094. test_async_cancel_on_handle_close
#     issues NtReadFile with an ApcRoutine on a handle bound to an I/O
#     completion port and wants STATUS_INVALID_PARAMETER; proskrnl accepted the
#     pair and answered 0x103. The rule is create_async's last statement
#     (server/async.c: `if (async->completion && data->apc)`), and its POSITION
#     is the content — the caller's event has already been reset above the
#     guard, the file object has NOT been cleared because that is queue_async's
#     below it, and the refusal precedes every verb, so an FSCTL_PIPE_PEEK that
#     would never have queued is refused just the same. Its reach is the
#     requests the SERVER queues, not the syscall's arguments: a regular disk
#     file's read never reaches wineserver (ntdll pread()s it), so the same
#     combination is admitted there and ntdll resolves it the other way with
#     `cvalue = apc ? 0 : apc_user` — the APC runs, no packet is posted — which
#     is why the refusal lives in rw.c's DEVICE branch, ioctl.c and notify.c's
#     watch arm rather than in NtReadFile. Landed in kernel/io/{async,rw,ioctl,
#     notify}.c, pinned by tests/ntapi/sem_port/port_apc.c; docs/21 W11,
#     docs/03 "A completion PORT and a completion APC ROUTINE".
#     2377 tests executed, 31 todo markers and 0 skipped before and after, and
#     a line-by-line histogram diff shows only :3094 removed with no other line
#     moved — so nothing was hiding behind the eight (docs/21 §4 trap 2 does
#     not apply here).
#
#     THE RELATIVE-ROOT CREATE AND OPEN ARE NOW BUILT: 60 -> 37, the 23 at
#     :2842-:2936, and the diagnosis above was right — one cause, 23
#     consequences. Two rules, both read off the oracle and both about WHICH
#     ROOT a call accepts rather than about npfs:
#       * an EMPTY name makes an UNNAMED pipe, and the root only has to
#         EXIST. server/object.c create_named_object's empty-name arm
#         allocates an unlinked object and never reads the parent, so
#         DECL_HANDLER(create_named_pipe)'s `get_handle_obj( process,
#         rootdir, 0, NULL )` is any type with no access — an EVENT handle is
#         a legal root — and only a handle naming nothing is refused. No root
#         at all is STATUS_OBJECT_PATH_SYNTAX_BAD, and FILE_OPEN of an empty
#         name resolves to the ROOT ITSELF and type-checks it, which no root
#         ever passes: STATUS_OBJECT_TYPE_MISMATCH.
#       * a NAMED create's root must reach the pipe DEVICE's own flat
#         namespace, and that is decided in the LINK (named_pipe_link_name
#         folds the device's root directory onto the device and refuses every
#         other parent with STATUS_OBJECT_NAME_INVALID) — so the name lands
#         in the device's namespace whatever the root was, which is why
#         "test3\pipe" is one KEY rather than a path and is then openable by
#         its absolute name.
#       The unnamed pipe's client end is reachable only through its SERVER
#       END's handle with an empty name (pipe_server_lookup_name resolves it
#       to the end, whose open_file tail-calls the same named_pipe_open_file
#       a by-name open reaches), and the client end is not a root in turn —
#       no_lookup_name's two arms give OBJECT_TYPE_MISMATCH for an empty name
#       and OBJECT_NAME_NOT_FOUND for a non-empty one. Landed in
#       fs/npfs/pipe.c + kernel/io/file.c, pinned by
#       tests/ntapi/sem_pipe/pipe_root.c; docs/21 W11, docs/03 "What ROOT a
#       named-pipe create and a pipe-relative open accept".
#       2377 tests executed, 31 todo markers and 0 flaky before and after, and
#       a line-by-line histogram diff shows exactly those 23 lines removed
#       with no other line moved — nothing was hiding behind them (docs/21 §4
#       trap 2 does not apply here).
#
#     THE DEVICE-ROOT FSCTL LADDER IS NOW BUILT: 37 -> 29, the eight at :2751
#     (four rows, asked once through `\Device\NamedPipe` and once through
#     `\Device\NamedPipe\`). The whole rule is one switch,
#     server/named_pipe.c named_pipe_device_ioctl, and its three answers do
#     NOT collapse into one "wrong object for this verb" — which is the
#     reading proskrnl had, answering STATUS_ILLEGAL_FUNCTION or a loud
#     STATUS_NOT_SUPPORTED to the lot:
#       * LISTEN and IMPERSONATE are ILLEGAL_FUNCTION — verbs the device does
#         not HAVE;
#       * DISCONNECT and TRANSCEIVE are PIPE_DISCONNECTED — a STATE it is not
#         in;
#       * QUERY_CLIENT_PROCESS is INVALID_PARAMETER, decided above any look
#         at the arguments (proskrnl answered 0xc00000bb);
#       * everything else — FSCTL_PIPE_PEEK INCLUDED, which the switch never
#         names — falls to default_fd_ioctl's own default (server/fd.c),
#         NOT_SUPPORTED. PEEK is the discriminating row and no winetest
#         assertion convicts it: :2721 asks the same call and is todo_wine
#         (it wants NT's INVALID_PARAMETER), so it is pinned because the
#         oracle answers it, not because anything failed.
#       FSCTL_PIPE_WAIT is the one arm where the device and its root
#       DIRECTORY part, and it is NOT built: see :2787 below.
#       Landed in fs/npfs/pipe.c, pinned by
#       tests/ntapi/sem_pipe/device_ioctl.c; docs/21 W11, docs/03 "What the
#       named-pipe DEVICE ROOT answers to each pipe FSCTL".
#       2377 tests executed, 31 todo markers and 0 flaky before and after, and
#       a line-by-line histogram diff shows exactly those eight removed with
#       no other line moved — nothing was hiding behind them (docs/21 §4
#       trap 2 does not apply here).
#
#     THE PIPE'S OWN LIFETIME IS NOW BUILT: 29 -> 22. SIX of those seven are
#     the fix and the seventh is a TRADE, so read it as 29 -> 23 on the item
#     plus one more off the top for matching the oracle where it differs from
#     NT. The previous revision's diagnosis was right in every particular — one
#     cause, six named consequences — and its "cannot go green while proskrnl
#     is the more NT-correct runner" note on :2185 was right too: the oracle
#     refuses FileNameInformation on a closing client under its own
#     `/* FIXME: We should be able to return on unlinked pipe */`, the winetest
#     wraps the SUCCESS assertion in todo_wine_if because real NT reports the
#     name, and proskrnl reported it. Adopting the oracle's refusal moves that
#     line FARTHER from NT and takes a failure off the count for doing so —
#     the same trade docs/03 records for DeletePending, and forced besides,
#     because "report the name" is un-pinnable (the oracle refuses, so no G5
#     case can be green on it, and beyond_oracle is barred for behaviour Wine
#     DOES implement). What the old block missed was not the ceiling but the
#     CAUSE: :2185 is the NAME half of the very lifetime the other six are the
#     OBJECT half of.
#     The oracle's pipe has two: every END references the object
#     (init_pipe_end's grab_object, released in pipe_end_destroy), while the
#     NAME goes with the last INSTANCE (pipe_server_destroy's
#     `if (!--pipe->instances) unlink_named_object`). proskrnl freed the whole
#     NPFS_PIPE at the last server end's cleanup, so a client that outlived its
#     server described a pipe that was gone. The pointer moved onto the END
#     (NPFS_END.pipe, over a reference count) and the two teardowns are now
#     separate — NpfsUnlinkPipe at instanceCount 0, NpfsDereferencePipe per
#     end. That also collapses `end->orphaned` into `end->pipe == 0`, which is
#     the quantity every `if (!pipe)` in the oracle's handler asks about.
#     Landed in fs/npfs/pipe.c, pinned by tests/ntapi/sem_pipe/pipe_lifetime.c
#     (and the todo_proskrnl tags in sem_pipe/{peek_state,pipe_file_info}.c are
#     gone); docs/21 W11, docs/03 "How long a pipe outlives its NAME".
#     THE EXECUTED COUNT ROSE 2377 -> 2378 and the todo markers 31 -> 32, both
#     accounted for per line: the +1 executed is test_pipe_with_data_state's
#     `if (status == STATUS_BUFFER_OVERFLOW)` inner ok() now RUNNING for the
#     closing client (the peek used to answer SUCCESS), and it passes; the +1
#     todo is :2185 moving from "succeeded inside todo" — which counts as a
#     failure — to a todo that fails as expected, i.e. the trade above and not
#     the item. A line-by-line histogram diff shows exactly
#     :2185/:2205/:2356/:2357/:2359/:2366/:2369 removed and no other line
#     moved.
#
#     WHAT A FILE HANDLE IS CALLED IS NOW BUILT: 22 -> 20, the two at :2849
#     and :2881. Both are NtQueryObject(ObjectNameInformation) on an UNNAMED
#     pipe end, which must be STATUS_OBJECT_PATH_INVALID; proskrnl answered
#     SUCCESS with an EMPTY name — and did so for every FILE handle in the
#     system, because IoFileObjectType had no OBJECT_TYPE.queryName hook and
#     Ob's generic walk finds no name for an object that was never linked into
#     the namespace. The DEVICE is linked; everything past it belongs to that
#     device, so the name is composed in ONE place (kernel/io/file.c
#     IopQueryFileObjectName: the device object's own name plus the
#     volume-relative path the FS's QueryName already builds for
#     FileNameInformation), and a filesystem's only say is whether its files
#     are in a namespace at all (IO_VFS_OPS.namedInObjectNamespace, TRUE for
#     fat32 and npfs). A device that HAS a namespace but no path for THIS file
#     refuses; a device with no namespace answers the empty name, which is not
#     a gap but the oracle's own no_get_full_name reply (the console objects
#     carry it). Landed in kernel/{io/file.c,ob/{ob.h,handle.c}}, pinned by
#     tests/ntapi/sem_pipe/object_name.c; docs/21 W11, docs/03 "What a FILE
#     handle is called in the object namespace".
#     2378 tests executed, 32 todo markers and 0 flaky before and after, and a
#     line-by-line histogram diff shows exactly :2849 and :2881 removed with no
#     other line moved — nothing was hiding behind them (docs/21 §4 trap 2 does
#     not apply here).
#
#     WHOSE SECURITY DESCRIPTOR A PIPE HANDLE REPORTS IS NOW BUILT: 20 -> 10,
#     all ten of test_security_info. The cluster was one subject and it was not
#     an npfs one: a pipe END has no descriptor of its own, it DEFERS to its
#     PIPE (server/named_pipe.c pipe_end_get_sd/pipe_end_set_sd, both
#     `if (pipe_end->pipe) return default_{get,set}_sd( &pipe_end->pipe->obj )`),
#     so both ends of every instance of one name share a single blob and an end
#     whose pipe is gone REFUSES with STATUS_PIPE_DISCONNECTED. proskrnl kept it
#     on the FILE OBJECT, i.e. one per open handle — which passes every
#     set-then-query-the-same-handle case and fails the moment a second end or a
#     second instance asks, which is exactly what the ten were. Ob's object type
#     grew a REDIRECT (OBJECT_TYPE.securityStorage: which slot, not what it
#     says, so Se keeps its one reading of an SD) and npfs points it at the
#     NPFS_PIPE. The create-time OBJECT_ATTRIBUTES descriptor came with it, in
#     the arm that MINTS the pipe only — a further instance drops its own, as
#     the oracle's `if (sd) default_set_sd(...)` sits inside "initialize it if
#     it didn't already exist". Landed in kernel/{ob,se,io}/ + fs/npfs/pipe.c,
#     pinned by tests/ntapi/sem_pipe/pipe_security.c (and sem_se/se_secobj.c for
#     the create-time defaulting, which is shared with every Ob object);
#     docs/21 W11, docs/03 "Whose security descriptor a pipe handle reports".
#     2378 tests executed, 32 todo markers and 0 flaky before and after, and a
#     line-by-line histogram diff shows exactly :2605/:2611/:2620/:2621/:2625/
#     :2632/:2638/:2641/:2643/:2664 removed with no other line moved — nothing
#     was hiding behind them (docs/21 §4 trap 2 does not apply here).
#
#     WHAT A CLOSE DOES TO A PARKED REQUEST IS NOW BUILT: 10 -> 5, all five of
#     test_async_cancel_on_handle_close (:3114x4 + :3111). The residue really
#     was ONE subject and the old block's decomposition of it was right; what
#     it did not have was the cause, which is neither npfs's nor the packet
#     path's. A process closing the LAST handle IT holds on a pipe end — while
#     another process's duplicate keeps the end alive — cancels the requests it
#     left parked there. The rule is async_close_obj_handle (server/async.c),
#     the close_handle op of both pipe-end object types and of sockets: two
#     counts (`obj->handle_count == 1 ||
#     get_obj_handle_count( process, obj ) != 1` -> do nothing) and a
#     three-term predicate (`!async->completion || !async->data.apc_context ||
#     async->event` -> skip). That is exactly the winetest's sixteen-row matrix:
#     only `other_process && apc_context && !event` reaches a cancel, and the
#     fifteen rows that do not are as much of the contract as the one that
#     does. Landed in kernel/ob/{ob.h,handle.c} + kernel/io/{io.h,async.c,
#     file.c,vfs.h} + fs/npfs/pipe.c, pinned by
#     tests/ntapi/sem_pipe/close_cancel.c; docs/21 W11, docs/03 "What CLOSING a
#     handle does to the requests that process left parked".
#     Three things it settles that no assertion in this pair asks for:
#       * OBJECT_TYPE.closeProcedure now takes NT's OB_CLOSE_METHOD shape and
#         fires on EVERY close, because "is this the last handle THIS PROCESS
#         holds" has no other answer. The three other hooks (job, debug object,
#         registry key) open with `if (systemHandleCount != 1) return;`, which
#         is the statement they used to make by being called nowhere else.
#       * "was this request port-bound" is asked of a DIFFERENT INSTANT than
#         "which port gets the packet". The packet's port is re-read at
#         completion (pending_packet.c §4), so a bind landing while a request
#         is parked still gets a packet; this predicate reads what create_async
#         captured at ISSUE and never re-reads, so the same late bind does NOT
#         arm the sweep. IOP_PENDING_REQUEST.portBoundAtIssue exists for that
#         one difference and close_cancel.c §6 is what separates them.
#       * where the rule STOPS is decided by which devices can PARK, not by a
#         branch: the sweep goes through IO_VFS_OPS.CancelPending, so npfs is
#         swept and fat32 is not — and directory watches deliberately are not,
#         because server/change.c dir_close_handle releases a cache entry and
#         nothing else.
#     THE OLD BLOCK'S NOTE ON :3111 WAS TRUE ABOUT THE ASSERTION AND WRONG
#     ABOUT THE COUNT. It said the succeeded-todo "cannot go green while
#     proskrnl is the more NT-correct runner", and that is still so — it did
#     not go green. It stopped being a FAILURE, because the cancel writes the
#     caller's IOSB the way the oracle does and the todo now fails as expected.
#     Windows leaves the block untouched until NtRemoveIoCompletion, which is
#     why the winetest wraps it in todo_wine_if; adopting the oracle's answer
#     moved that one line FARTHER from NT and took a failure off the count for
#     doing so. Same trade as the pipe-lifetime item's :2185 and as
#     STATUS_USER_APC (Art. 6). So read the pair as 10 -> 6 on the fix and one
#     more off the top for matching the oracle where it differs from NT.
#     2378 tests executed, 0 flaky and 0 skipped before and after; the todo
#     markers move 32 -> 33, which is exactly :3111 crossing from
#     "succeeded inside todo" to a todo that fails, and a line-by-line
#     histogram diff removes exactly :3111 and :3114x4 with no other line
#     moved — so nothing was hiding behind the five (docs/21 §4 trap 2 does not
#     apply here).
#
#     WHAT IS LEFT, per-line histogram of the 5 (3 failed + 2 succeeded-
#     todo; a serial-log count that ignores the succeeded-todo lines reads 3,
#     and one that does not drop the console's redrawn first draft of a wrapped
#     line double-counts — docs/21 W16). These are read off the assertions and
#     grouped by test function; except where marked DIAGNOSED none of them is,
#     and docs/21 §4 trap 4 says the loudest is usually the consequence:
#       -  2  test_empty_name — what is left of the `\Device\NamedPipe`
#             device-root surface, and it is ONE item (the ioctl ladder and the
#             ObjectNameInformation hook that were the other two are built):
#               * :2810 and :2787 both need the device told from its root
#                 DIRECTORY, and neither is an npfs item. :2810: a NAMED
#                 create relative to a handle on `\Device\NamedPipe` (no
#                 trailing backslash) must be STATUS_OBJECT_NAME_INVALID, and
#                 proskrnl now answers SUCCESS where it used to answer
#                 0xc0000024 — i.e. it accepts where NT refuses, having
#                 previously refused everything (its failure MODE changed with
#                 the relative-root item above rather than its count). :2787:
#                 FSCTL_PIPE_WAIT on the DEVICE must be
#                 STATUS_ILLEGAL_FUNCTION, where on the directory it is the
#                 name lookup (OBJECT_NAME_NOT_FOUND for an absent pipe —
#                 that half already passes); it is the ONE arm where
#                 named_pipe_dir_ioctl does not tail-call
#                 named_pipe_device_ioctl, and the rest of that ladder is
#                 built (see above). Both roots open through NpfsVfsCreate's
#                 root arm and carry no pipe state, so nothing downstream can
#                 tell them apart; ObpLookupName hands the FS an empty
#                 remainder for both. That is docs/21 W7's parser question —
#                 the same one `\??\C:` vs `\??\C:\` asks, and W7's `\\.\c:`
#                 blocker — so those two are ONE kernel/ob item. And the
#                 ObjectNameInformation hook above did NOT move them, which is
#                 worth recording: proskrnl now reports `\Device\NamedPipe\`
#                 for the device root exactly as the oracle does, because it
#                 composes the DIRECTORY's answer — the one spelling it has.
#       -  2  subtest_pipe_name (:2969, succeeded-todo).
#       -  1  pipe.c:725 — NtCancelSynchronousIoFile(GetCurrentThread(), NULL,
#             (IO_STATUS_BLOCK *)0xdeadbeef) must answer
#             STATUS_ACCESS_VIOLATION; proskrnl answers 80000002
#             (STATUS_DATATYPE_MISALIGNMENT), because KiProbeForWrite tests
#             alignment before accessibility and 0xdeadbeef is neither. A
#             probe-ORDER question, not a pipe one, and still the pair's
#             smallest remaining item.
# TODO: Implement
# ntdll_test.exe:pipe
ntdll_test.exe:port
# TRIAGED, FIXABLE. 160 failures across 1042 tests, down from 172 across the
#     same 1042 — and 192 across 1050 the milestone before that. The executed
#     count did NOT move with the last 12 (the license block's ok()s are
#     unconditional, so clearing them changes the failure count and nothing
#     else), but it DID fall with the 192 -> 172, and that was not a loss
#     either: the six rename assertions below were per-ITERATION over a
#     subkey list that had one stale entry in it, so removing the entry
#     removes 4 iterations x 2 ok()s = exactly the 8 that went missing (4 of
#     them were failing at :2380, 4 were passing at :2367).
#
#     THE OLD BLOCK'S DIAGNOSIS OF ITS OWN LARGEST CLUSTER WAS WRONG, AND
#     THAT MATTERS MORE THAN THE NUMBER. It read the 24 failures at
#     reg.c:1354 as "NtCreateKey -> STATUS_OBJECT_PATH_SYNTAX_BAD ... a NAME
#     question". Measured: the call at :1348 is NtOpenKey (the message text
#     says NtCreateKey and is simply wrong), and the refusal is CORRECT. Its
#     OBJECT_ATTRIBUTES.RootDirectory is a handle an earlier NtOpenKey left
#     at zero, so the kernel is being asked to resolve a RELATIVE name with
#     no root — which really is STATUS_OBJECT_PATH_SYNTAX_BAD, on both
#     runners. Reading the failure TEXT rather than the test's helper is
#     docs/21 §4 trap 4 again.
#
#     What actually stops that cluster is registry FURNITURE the image does
#     not carry (docs/21 §4 trap 6): the oracle's prefix has
#     \Registry\Machine\Software\Classes\Interface and ...\Software\
#     Wow6432Node (wine.inf writes both), the baked hive has neither, and
#     NtCreateKey creates only the last component. reg.c:1780's open of
#     "Interface" fails, root64/root32/root become 0, and 48 assertions plus
#     the whole of test_redirection fall out behind that. It is a data
#     question with a decision inside it (may a kernel with no WOW64 carry a
#     Wow6432Node key?), NOT the name question this block advertised.
#
#     WHAT WAS FIXED: the name fold (docs/21 W12). reg.c:346 creates a key
#     named U+00F6 U+00F3 U+014D U+0371 U+D801 U+DC00 and :353 re-opens it
#     upper-cased; proskrnl's RtlUpcaseUnicodeChar was a hand-written rule
#     reaching only ASCII + Latin-1, so the open missed (:355) and :357's
#     delete was handed a null handle. That ONE miss cost 20 assertions,
#     because the undeleted subkey outlives its test: its parent
#     \Registry\User\<SID>\WineTest keeps a child forever, only a LEAF key
#     is deletable, and three later test functions are about that parent —
#     test_NtDeleteKey's whole deleted-key contract (:821-:851, 10), the
#     symlink test's delete (:1325), NtRenameKey's subkey enumeration
#     (:2363/:2380, 6), the hive-save delete (:2560) and
#     RtlQueryRegistryValues' call count (:3070). Not inferred — :2380
#     printed the leftover by name: `Unexpected subkey name
#     L"\00f6\00f3\014d\0371\d801\dc00"`, and :2363 `Expected 4 subkeys,
#     found 6`.
#     RtlUpcaseUnicodeChar now reads the NLS upcase trie generated from the
#     pinned tree's own nls/l_intl.nls (kernel/lib/upcase.h,
#     tools/gen_upcase.py) — the same bytes the oracle folds through, on
#     both of its sides — so it is total over the BMP and per 16-bit code
#     unit. Pinned by tests/ntapi/sem_reg/key_name_fold.c (the table's own
#     irregularities, and the surrogate halves that must NOT fold) and
#     sem_file/name_case_fold.c (the same fold through file names, and the
#     enumeration ORDER it decides).
#
#     WHAT WAS FIXED SINCE (docs/21 W12, the license furniture): the 12 at
#     :1009-:1032 are gone. wine.inf's [LicenseInformation] writes ELEVEN
#     values into Software\Wine\LicenseInformation and CmInitialize seeded
#     exactly one of them by hand, so "Kernel-MUI-Number-Allowed" — the
#     REG_DWORD the block below asks for — answered
#     STATUS_OBJECT_NAME_NOT_FOUND. The seed is now the WHOLE section,
#     generated out of the pinned wine.inf rather than transcribed
#     (kernel/cm/license.h, tools/gen_license.py; Makefile --check catches a
#     pin bump editing it). Pinned by tests/ntapi/sem_reg/license_value.c,
#     which repeats the size/type protocol against a REG_DWORD value —
#     the assertions that a one-value one-type seed passes.
#     A record corrected on the way: docs/16 still filed
#     NtQueryLicenseValue under "no consumer in the baked stack" although
#     table.inc had carried a KI_SYSCALL row for it since the syscall was
#     built; the tally was 202/264 and is 203/264.
#
#     WHAT WAS FIXED SINCE THAT (docs/21 W12, the namespace rules): the FOUR
#     singleton assertions below are gone — :550, :557, :854 and :1312 — and
#     they were three unrelated rules that happened to be small:
#       - :550/:557, "\REGISTRY\..." and "\REGISTRY\MACHINE\SOFTWARE\CLASSES"
#         with attr.Attributes = 0, answered 0xc000003a. The leading
#         "\Registry" component is an OBJECT-MANAGER name and Ob's walk honours
#         OBJ_CASE_INSENSITIVE, so a caller passing 0 was asking for a
#         case-SENSITIVE match on the one component of the path that Cm does
#         not compare (CmpFindSubkey never read the flag). The oracle folds it
#         in the half proskrnl REPLACES — dlls/ntdll/unix/registry.c ORs
#         OBJ_CASE_INSENSITIVE into both NtCreateKey's and NtOpenKeyEx's
#         attributes — so CmpResolvePath forces it now, through a new
#         ObpLookupParseObject argument rather than a second walk.
#         Pinned by tests/ntapi/sem_reg/root_case.c.
#       - :854, NtQueryObject(ObjectNameInformation) on a DELETED key handle,
#         answered success with an empty name. Keys are Cm tree nodes, so Ob's
#         generic name walk found no name for ANY key and reported every one of
#         them as nameless — the deleted case was the only assertion that
#         could see it. CmpKeyType now carries a queryName hook
#         (ob.h OBJECT_TYPE), the oracle's own key_ops.get_full_name shape:
#         the KeyNameInformation walk with a STATUS_KEY_DELETED guard in front,
#         taken before the buffer-size protocol.
#         Pinned by tests/ntapi/sem_reg/key_object_name.c.
#       - :1312, the symlink-loop open, answered STATUS_OBJECT_NAME_NOT_FOUND.
#         The refusal was right and its STATUS was not: the name is fine, the
#         request cannot be served, and the oracle's exhausted name walk says
#         STATUS_INVALID_PARAMETER (server/object.c lookup_named_object,
#         `recursion_count > 32` — the same bound on the same quantity as
#         CMP_MAX_LINK_EXPANSIONS). Pinned in sem_reg/symlink.c over BOTH loop
#         shapes: one whose target extends the path and a two-link cycle whose
#         does not, because only the second proves the guard counts follows
#         rather than length.
#     Verified by a line-by-line diff of the failing-assertion histogram: those
#     four lines removed, no other line moved, and 1042 tests executed with 16
#     todo markers before and after — none of the three rules sits under an
#     `if (status == ...)` that would change what runs.
#
#     WHAT IS LEFT, re-measured, summing to 156 — and ALL OF IT IS THE ONE
#     DATA QUESTION named above, so this pair now has no small items:
#       - 48  the Classes\Interface furniture above (:1354 x24, :1361 x24);
#       - 86  test_redirection and test_redirection_classes behind the
#         missing Software\Wow6432Node (41 at :1470-:1673, 30 at
#         :1781-:1879, 15 at :1901-:1970) — the same data question, and it
#         carries the decision named above;
#       - 22  the Wow6432Node enumeration group at :1980-:1990.
# TODO: Implement
# ntdll_test.exe:reg
# AT ITS CUI-IMAGE FLOOR — NOT FIXABLE ON THIS IMAGE.
#     All 26 failures are test_LdrAddRefDll (rtl.c:2314-:2348) and every one
#     follows from LoadLibraryA("comctl32.dll") returning NULL. comctl32
#     imports user32 and gdi32, so it cannot be on the CUI volume (G7).
#     Deliberately NOT excluded the way kernel32:toolhelp is: toolhelp's
#     entire subject sits behind its GUI dependency, while here the rest of
#     the pair passes. It is commented out because it cannot go green, not
#     because it convicts nothing.
# ntdll_test.exe:rtl
ntdll_test.exe:rtlbitmap
ntdll_test.exe:rtlstr
ntdll_test.exe:string
# RE-MEASURED, FIXABLE — and it WEDGES *after* reaching its own summary,
#     which narrows it a long way. The pair prints
#     "sync: 0 tests executed (0 marked as todo, 0 as flaky, 0 failures), 0
#     skipped" — with one todo marker at sync.c:856 above it — and only THEN
#     hangs to the per-pair timeout. So the subtest body runs to completion
#     and nothing it asserts fails; what never finishes is the PROCESS, i.e.
#     something is still parked at exit. Read the serial log first, then the
#     thread list.
#     NOT the same bug as ntdll:thread's wedge, which was measured and fixed
#     this session (THREAD_CREATE_FLAGS_BYPASS_PROCESS_FREEZE); this pair is
#     unchanged by that fix, so docs/21 §5's "the three wedges are probably
#     one bug" is refuted for this pair.
# TODO: Implement
# ntdll_test.exe:sync
# PARTLY FIXED, FIXABLE — the WEDGE IS GONE and the pair now stops on an
#     unbuilt syscall instead, which is progress with a new frontier.
#     What wedged it was test_thread_bypass_process_freeze
#     (dlls/ntdll/tests/thread.c): a thread created with
#     THREAD_CREATE_FLAGS_BYPASS_PROCESS_FREEZE calls NtSuspendProcess on its
#     OWN process and is then the only thread able to call NtResumeProcess.
#     proskrnl accepted the flag and froze that thread too, so the process
#     could never be resumed and the main thread's join never returned — a
#     silent 300 s deadlock with nothing on serial, because a wedge names
#     nothing. Fixed in kernel/ps/{thread,process}.c and pinned by
#     tests/ntapi/sem_ps/bypass_process_freeze.c (docs/21 W10).
#     The pair now runs on to test_NtQueueApcThreadEx and PANICS at the
#     missing NtQueueApcThreadEx (syscall 0xd3) at ~1.4 s. Zero failed
#     assertions before it. Behind that syscall sits NtAllocateReserveObject
#     and the reserve-object type the subtest asks for
#     (MemoryReserveObjectTypeUserApc / IoCompletion) — both filed under
#     docs/16 "Superseded / legacy forms — no consumer in the baked stack",
#     a row the winetest gate now contradicts exactly the way it does for
#     mailslots (docs/21 W8). That decision is the next thing this pair
#     needs, not more triage.
#     And per docs/21 §4 trap 2: everything past that panic — the rest of
#     test_NtQueueApcThreadEx and all of test_skip_thread_attach — has never
#     been measured. The zero above is a lower bound.
# TODO: Implement
# ntdll_test.exe:thread
# RE-MEASURED, FIXABLE — the old block ("a HANG with no output at all, not
#     one assertion runs") is STALE and was wrong about the shape. The pair
#     runs a long way: two ntdll threadpool fixmes (tp_object_initialize:
#     persistent threads, environment version 9999), then todo markers at
#     threadpool.c:652 (TpSimpleTryPost) and :1622 ("expected that timers are
#     merged"), and only then hangs to the per-pair timeout. No failed
#     assertion, no unbuilt class, no user fault is named.
#     Measured again after ntdll:thread's wedge was fixed this session and
#     unchanged by it, so this is its own bug — the threadpool is PE-side in
#     ntdll, and what fails underneath is still most likely the timer/wait/APC
#     surface it is built on. Start at what runs after :1622.
# TODO: Implement
# ntdll_test.exe:threadpool
# ORACLE-FLAKY, NOT A KERNEL GAP — and the only entry here whose PROSKRNL
#     leg is green. `wtest ntdll_test.exe:time PASS`; the ORACLE leg fails,
#     and only on a loaded shared runner (it is 0-failing on a quiet host).
#
#     The cause is in the test, not in either runner. time.c:457 reads the
#     KUSER_SHARED_DATA SystemTime and NtQuerySystemTime back to back and
#     guards the ordering assertion with
#
#         todo_wine_if(t1 > t2 && t1 - t2 < 50 * TICKSPERMSEC)
#
#     — a todo whose CONDITION is itself the race. Under load the skew
#     between the two reads exceeds 50 ms, the todo stops applying, and the
#     ok() underneath becomes a hard failure. Nothing proskrnl does can
#     change that: it is Wine's own USD-vs-syscall clock skew on the host.
#
#     Commented out rather than left active because a gate that goes red on
#     host scheduling teaches the next person to ignore it. Re-activating it
#     needs a decision about the oracle leg (tolerate a named flaky subtest,
#     or retry it), not a kernel change — which is why there is no
#     `TODO: Implement` line.
# ntdll_test.exe:time
ntdll_test.exe:unwind
ntdll_test.exe:virtual
ntdll_test.exe:wow64

# --- kernel32 (third_party/wine/dlls/kernel32/tests) ------------------------
# UNTRIAGED, FIXABLE. 39 failures in five sites, and the shape says
#     "write a PE to disk, then run or map it": 26 at actctx.c:4354
#     (subtest_manifest_res, which writes an EXE+DLL pair and executes them),
#     8 at :4475, 3 at :3151 (LoadLibraryExA -> error 2), and one each at
#     :2572/:3955 (CreateActCtxA -> 14001). kernel32:resource fails the same
#     way and so does part of ntdll:rtl; the three are worth one triage
#     rather than three, and the hypothesis to test is a section/page-cache
#     coherence problem on the SEC_IMAGE path for a file just written.
# TODO: Implement
# kernel32_test.exe:actctx
kernel32_test.exe:atom
kernel32_test.exe:change
kernel32_test.exe:codepage
kernel32_test.exe:comm
# (b) Needs a REAL console, which the oracle cannot give it: the runner
#     redirects stdout to a log, so console.c:5920 fails "Getting sb info"
#     on the pinned Wine (1 failure). proskrnl runs the pair WITH conhost,
#     so the two legs are not comparing the same thing. Attaching a pty
#     (script -qec) does hand wine a console but it then dies on
#     "Unable to open HKCU\Console, error 2" and fills the log with cursor
#     escapes — measured, not assumed.
# kernel32_test.exe:console
# (a) The debug-object EVENT QUEUE is unbuilt, so proskrnl panics as soon
#     as the pair reaches it — via NtWaitForDebugEvent, or earlier at
#     CreateProcess(DEBUG_PROCESS), which NtCreateUserProcess still
#     refuses; which comes first is not re-measured here, only that both
#     are queue surface. Green on the oracle. This block used to say
#     "out of scope permanently, by decision, not by backlog" (ADR 0011) —
#     that is withdrawn: ADR 0011 is DEPRECATED and the queue is in-scope
#     unbuilt work, and this pair is the named consumer for it. The pair
#     stays parked because the ids refuse, not because the surface is
#     ruled out; un-park it when the queue lands. Attach itself IS built
#     (Amendment 1), so NtCreateDebugObject is no longer where it dies.
# kernel32_test.exe:debugger
kernel32_test.exe:directory
kernel32_test.exe:drive
# RED AND FIXABLE — and newly visible, not newly broken. 1 failure:
#     environ.c:74 `Failed to get user profile dir, error 2`
#     (ERROR_FILE_NOT_FOUND), in test_Predefined. The pair was GREEN while
#     the leg baked its own short DLL list, because that list had no
#     userenv.dll: the test resolves GetUserProfileDirectoryA by
#     GetProcAddress and SKIPS the whole check when the DLL is absent. The
#     image is the `make run` machine now (docs/03 "M10 winetest notes"),
#     userenv.dll is on it as it is on the product, and the check runs.
#     THE CAUSE is machine state no proskrnl image has: userenv's
#     GetUserProfileDirectoryW reads
#     HKLM\Software\Microsoft\Windows NT\CurrentVersion\ProfileList
#     "ProfilesDirectory", and on the oracle's prefix that value (and the
#     per-SID "ProfileImagePath" beside it) is written during
#     `wineboot --init` by SHELL32 — wineboot's update_user_profile calls
#     SHGetSpecialFolderPathW, and shell32's _SHGetProfilesValue writes the
#     C:\users default on first query. shell32 is the GUI stack (Art. 7),
#     off every CUI image, so firstboot runs here and that half of it does
#     not. proskrnl is internally consistent about it — the default
#     environment (kernel/ps/peb.c) has no USERPROFILE either — so the fix
#     is a user profile, not a patch to this one value: seed ProfileList
#     (ProfilesDirectory + ProfileImagePath for the fixed Se identity),
#     create C:\users\<name>, and add USERPROFILE to the default
#     environment. That is kernel/Cm work with its own G5 pin, so the pair
#     is parked rather than left red.
# TODO: Implement
# kernel32_test.exe:environ
kernel32_test.exe:fiber
# PARTLY FIXED, FIXABLE. 38 failures, down from a re-measured 164 — and both
#     numbers are counted, not carried over. The block this replaces said 152,
#     which does not reproduce: the merge base measures 164 with the same
#     binary, of which 14 are "Test succeeded inside todo block" (winetest
#     scores a succeeded todo as a failure) and 150 are "Test failed".
#     TWO WAYS TO MISCOUNT THIS LOG, both paid for here:
#       - grepping "Test failed" misses the succeeded-todo lines entirely (the
#         same reading error kernel32:profile's block records at :229);
#       - the proskrnl leg's console REDRAWS a line that wraps — it prints
#         "file.c:848:Test failed:", then a bare CR, then the whole line
#         again — so a per-line counter that does not drop the empty first
#         draft reports one assertion twice. That is where an earlier draft of
#         this block's "166" came from.
#
#     WHAT WENT: 126 assertions, ONE subject — the hold a live SECTION keeps
#     on its file. NT models a section as a pseudo-open carrying magic access
#     bits (wine server/file.h:303-305 FILE_MAPPING_ACCESS / _IMAGE / _WRITE,
#     set in server/mapping.c create_mapping) that server/fd.c check_sharing
#     reads back as four rules. proskrnl had one of the four, and had it
#     reading the wrong access bit:
#       :2672 x96  a PAGE_READWRITE data section demands FILE_SHARE_WRITE of
#                  every later open — whatever access that open asks for,
#                  INCLUDING none, because the rule sits ABOVE check_sharing's
#                  own "no data access -> sharing ignored" escape. proskrnl's
#                  IoCheckShareAccess opened with that escape, which is why
#                  the whole a2 == 0 column was exempt;
#       :2663 x16  the image rule is `access & FILE_WRITE_DATA` and nothing
#                  else. proskrnl grouped FILE_APPEND_DATA with it the way
#                  every OTHER share-mode rule in NT does, so the two
#                  append-only columns of the SEC_IMAGE row were refused;
#       :2695 x3 + :2696 x3   a truncating disposition (FILE_SUPERSEDE /
#                  FILE_OVERWRITE / FILE_OVERWRITE_IF, the oracle's three
#                  O_TRUNC dispositions, server/file.c:233-:241) over ANY live
#                  section is STATUS_USER_MAPPED_FILE;
#       :2707/:2708  FILE_DELETE_ON_CLOSE over a SEC_IMAGE section is
#                  STATUS_CANNOT_DELETE — a third status, not a sharing
#                  violation.
#     SIX MORE were outside test_file_sharing entirely and read as three
#     unrelated Win32 symptoms; all three are the same two rules reached
#     through a Win32 spelling of an open:
#       :847/:848    CopyFile onto a MAPPED destination (CREATE_ALWAYS ->
#                  ERROR_USER_MAPPED_FILE);
#       :1174/:1175  the same through CopyFile2;
#       :1931/:1932  DeleteFile of a MAPPED IMAGE (an open carrying
#                  FILE_DELETE_ON_CLOSE -> ERROR_ACCESS_DENIED).
#     The old block filed :847 and :1174 under "a handful around CopyFile of a
#     mapped or self-named file" and did not have :1931 at all.
#     The ORDER between the four rules is observable and is the oracle's, not
#     a tidy one: write beats truncate, delete beats truncate, and the section
#     rules sit BETWEEN check_sharing's two share-mode directions. Landed in
#     kernel/io/file.c (IoCheckShareAccess over three IO_FCB counters), pinned
#     by tests/ntapi/sem_mm/section_file_hold.c; docs/21 W16, docs/03 "What a
#     live SECTION holds against a later OPEN".
#
#     WHAT IS LEFT, re-measured, summing to 38:
#       - 16  :3208, FindFirstFile with the "*." pattern — a FAT short-name
#         question, and the largest single item left here;
#       - 14  succeeded-todo markers at :388/:394/:396/:398, :1409,
#         :1557/:1561/:1578, :2087, :2132 and :3321/:3323/:3333/:3335, each a
#         place proskrnl is MORE NT-correct than the pinned Wine the todo was
#         calibrated against. Read them before treating them as work: the same
#         shape as kernel32:volume:186 and kernel32:profile:229, where the
#         pair cannot go green while proskrnl is the better of the two
#         runners;
#       -  3  :702 and :1006/:1007, CopyFile/CopyFile2 of a file to ITSELF,
#         which must fail ERROR_SHARING_VIOLATION and does not. Same file
#         opened twice, no section involved — a plain share-mode question and
#         the cheapest item on this list;
#       -  5  singletons: :1403, :1884, :2398, :2806, :3269.
#
#     AND 38 IS STILL A LOWER BOUND: the run PANICS at file.c:3338's
#     CreateMailslotW and never reaches its summary line — so, like
#     ntdll:file, this pair is gated on mailslots (docs/21 W8) and nothing
#     past :3338 has ever been counted (docs/21 §4 trap 2). It stops in
#     exactly the same place it did before this session, so 164 -> 38 is a
#     like-for-like comparison of the same measured prefix.
# TODO: Implement
# kernel32_test.exe:file
kernel32_test.exe:format_msg
kernel32_test.exe:generated
kernel32_test.exe:heap
# (b) THE ORACLE STILL PRODUCES NO VERDICT — re-measured at the WOW64
#     milestone, deliberately, because this pair was the milestone's stretch
#     goal and the obvious hypothesis was that it needed the 32-bit
#     subsystem the oracle build lacked. It does not: with i386 in
#     WINE_CONFIGURE the oracle still ends in its child-hang process dump
#     with NO summary line, and the fault is not in the debug loop at all —
#     it is an unhandled access at loader.c:4868 (`test_dll_file`, probing
#     user32.dll: `movzwl 6(%rsi), %r13d`). Zero "Test failed" lines before
#     it, i.e. the run dies rather than fails.
#
#     Under Art. 6 that settles it: a pair whose oracle never reaches a
#     verdict convicts nothing on either side, so the proskrnl half is
#     unconvictable no matter what it does. For the record, what it does is
#     panic at NtWaitForDebugEvent — the debug EVENT QUEUE, unbuilt and
#     refusing loudly (in scope, not excluded; ADR 0011 is deprecated) — with real
#     failures at loader.c:4658-:4678 ahead of it. Neither observation is
#     actionable while the oracle is silent.
#
#     Re-opening this is a decision about the ORACLE (fix or work around the
#     loader.c:4868 fault upstream), not a kernel change, which is why there
#     is no TODO.
# kernel32_test.exe:loader
# UNTRIAGED, AND PROBABLY NOT KERNEL WORK. 94 failures: 72 at
#     locale.c:3809/:3813 (LCMAP_SORTKEY and CompareStringEx collation), 11
#     at :2308 (CompareString order), 8 at :7954, 3 GUID checks at
#     :7993-:8042. Collation is resolved PE-side out of sortdefault.nls,
#     which IS on the boot volume, so the kernel's part is at most the
#     file/section path that maps it. Confirm the NLS file maps identically
#     on both runners before spending an hour here; if it does, the failure
#     is in Wine PE code and is not proskrnl's to fix — and G9 forbids
#     patching Wine to make it pass.
# TODO: Implement
# kernel32_test.exe:locale
# GATED ON A WHOLE SUBSYSTEM, FIXABLE. Dies at the very first thing it
#     does: CreateMailslot has nothing behind it, returns
#     INVALID_HANDLE_VALUE, and the test then calls GetMailslotInfo on that
#     -1 — reaching NtQueryInformationFile class 26
#     (FileMailslotQueryInformation), which is unbuilt and panics. So the
#     pair measures nothing at all today. The work is docs/21 W8:
#     NtCreateMailslotFile plus the mailslot device plus classes 26/27.
#     ntdll:file is blocked by the same thing, and note docs/16 still files
#     NtCreateMailslotFile under "no consumer in the baked stack" — that row
#     is now false and moves with the implementation.
# TODO: Implement
# kernel32_test.exe:mailslot
# (b) Asserts the TEST BINARY'S OWN import list, which the standalone link
#     deliberately changes: module.c:1774 walks the dependencies and wants
#     user32.dll where our link has advapi32, because user32 is stood in at
#     link time by tests/winetest/glue/user32_stubs.c (docs/03 "One binary,
#     two runners"). 2 failures on the oracle, identical on proskrnl — it
#     measures the harness, not the boundary.
# kernel32_test.exe:module
kernel32_test.exe:path
# PARTLY FIXED, FIXABLE. 28-34 failures across four runs, down from a
#     re-measured 110.
#
#     FIRST, THE COUNT IS A LOWER BOUND AND ALWAYS WAS — this block did not
#     say so and the previous two revisions were read as if it were a total.
#     :845 is alarmThreadMain, and what it does after `ok(FALSE, "alarm")` is
#     ExitProcess(1). So this pair is KILLED 20 s in, prints no summary line
#     ("N tests executed" never appears; the runner reports exit=0x1), and
#     everything past the kill has never been counted (docs/21 §4 trap 2 —
#     the pair belongs on that list and was not on it). The oracle executes
#     38468 tests with 0 failures; proskrnl does not reach its own count.
#     That is also why the total moved 93 -> 96 -> 94 -> 110 across sessions
#     without a matching kernel change: the 20 s deadline cuts the run at a
#     different place each time, so the total is a property of the DEADLINE
#     as much as of the kernel. Its per-line breakdown is the yardstick; the
#     total is not, and comparing two totals is only like-for-like when the
#     run stops in the same place.
#
#     WHAT WENT: 82 assertions, ONE cause, and it was not the one this block
#     named. The block called the 76-assertion cluster at :1404/:1405/:1406
#     "fs/npfs territory ... the overlapped half is async-completion shaped".
#     The cluster starts in serverThreadMain1 and 2, which are PIPE_WAIT byte
#     echo servers with no overlap anywhere in them, and every server-side
#     assertion in both was already passing. The cause was
#     NtFlushBuffersFile: on a pipe it must not return until the peer has
#     consumed what this end wrote (wine server/named_pipe.c pipe_end_flush),
#     and kernel/io/rw.c answered a no-op STATUS_SUCCESS for every cache-less
#     device. So `FlushFileBuffers(hnp); DisconnectNamedPipe(hnp)` discarded
#     the echo before the client's ReadFile ever ran — the client's read is
#     what fails, three assertions per loop, and the server's own four pass.
#     docs/21 W11, docs/03 "What a pipe's NtFlushBuffersFile waits for",
#     pinned by tests/ntapi/sem_pipe/flush_buffers.c. Both synchronous echo
#     servers are now clean end to end (16 loops, 0 failures).
#
#     WHAT WENT SINCE: the OVERLAPPED server's own five, :1059/:1077/:1083/
#     :1101/:1106 (serverThreadMain3's overlapped ReadFile/WriteFile and the
#     DisconnectNamedPipe behind them). Their cause was the ntdll:pipe wedge —
#     npfs data reads never pended, so an overlapped read on an empty pipe
#     parked the server thread instead of answering STATUS_PENDING. Built
#     (docs/21 W4c, docs/03 "CUI-8 async notes", pinned
#     tests/ntapi/sem_pipe/pended_read.c) and all five are gone.
#
#     WHAT IS LEFT, re-measured, summing to 29 — and the total is STILL a lower
#     bound, for the same reason it always was: :845 kills the process at 20 s,
#     so the count is a property of the DEADLINE as much as of the kernel.
#       - 26  :1396 x6 and :1401/:1402/:1404/:1405 x5, the
#         CLIENT failing to open the pipe at all and everything behind the
#         handle it never got, once per exerciseServer loop until the deadline.
#         UNCHANGED by the fix above, which is the finding: the previous
#         revision of this block called it "a consequence" of the overlapped
#         server's five, and it is not — those are fixed and this stands. What
#         it IS has not been established. The log names serverThreadMain2 last,
#         but that is only because serverThreadMain2's trace is the one
#         unconditional trace() in the file (1/2/3/4/5 all print under
#         winetest_debug > 1), so it does not identify which server's
#         exerciseServer is stuck. Do not re-attribute it without measuring;
#       -  1  :845, the watchdog firing at 20 s because of the above;
#       -  2  :1527/:1533, WaitForSingleObject(hFile, 0) wanting WAIT_TIMEOUT
#         and PeekNamedPipe wanting ERROR_BAD_PIPE after a DisconnectNamedPipe.
#         The first is the file-object signal again from the other side: the
#         oracle leaves a handle UNSIGNALLED after a request that failed
#         without pending (async_set_result signals only under
#         `pending || !NT_ERROR`), and proskrnl's object is simply still up.
# TODO: Implement
# kernel32_test.exe:pipe
kernel32_test.exe:power
# (b) The console dependency again, and the largest instance of it: 48
#     failures on the oracle, starting at process.c:1650 "Getting sb info" /
#     "Getting console in mode" — the STARTUPINFO console-handle block needs
#     a console the redirected oracle has not got.
# kernel32_test.exe:process
# TRIAGED TO ITS FLOOR — NOT FIXABLE WHILE THE BACKEND IS FAT32.
#     4 failures, down from a re-measured SEVEN. The block this replaces
#     claimed 6 and guessed at the cause of both halves; it was wrong about
#     the count and about both causes. (The seventh is profile.c:229, which
#     it never listed: it is a todo_wine that PASSES on proskrnl, and
#     winetest counts a succeeded todo as a failure — easy to miss when
#     scanning a log for "Test failed".)
#
#     The first half is GONE and was never a .ini round trip. profile.c:95
#     and :101 are test<00> of test_profile_int, which is
#     GetPrivateProfileIntA(NULL, NULL, 70, NULL) — every argument NULL, so
#     kernel32 resolves the file to %windir%\win.ini and the section to "the
#     section NAMES in it" (dlls/kernel32/profile.c GetPrivateProfileStringW,
#     the !section arm). With win.ini present that returns "mail", which
#     parses as 0; with it absent the call returns 0 and the API hands back
#     the default 70. The wtest image had no win.ini because it carries no
#     wineboot.exe, so smss skips firstboot and the `wineboot --init` pass
#     that writes it (wine.inf [SystemIni], UpdateInis) never ran — while the
#     oracle leg runs in a wineprefix where it did. The image now bakes
#     win.ini + system.ini generated from that same [SystemIni] payload
#     (tools/gen_sysini.py, byte-identical to a prefix's own copies), which
#     also cleared :229, where proskrnl's ERROR_FILE_NOT_FOUND was passing a
#     todo_wine that the oracle fails.
#
#     What is left is ONE cause and it is the FAT floor.
#     test_profile_directory_readonly (:536) creates a directory whose DACL
#     grants Everyone read+execute only, then requires the three
#     WritePrivateProfileString* calls into it to FAIL (:559/:562/:565) and
#     RemoveDirectoryA to then succeed (:568 — it fails 145 on the file the
#     writes left). That needs a volume that STORES and ENFORCES ACLs.
#     Security descriptors are an NTFS construct; FAT32 has nowhere to put
#     one, and proskrnl's volume says so — fs/fat32/fat.c reports
#     FILE_CASE_PRESERVED_NAMES and not FILE_PERSISTENT_ACLS, the documented
#     flag for "preserves and enforces ACLs" (GetVolumeInformation). NT on a
#     FAT volume answers this test the same way proskrnl does. The oracle
#     passes only because Wine maps the DACL onto unix permission bits —
#     which is why the same 4 went "from 4 to 0" when the oracle stopped
#     running as root (docs/03 "M10 winetest notes"; that run logged the
#     count and not the lines, so the two sets being identical is an
#     inference from count plus mechanism). Same floor as
#     ntdll:directory's reparse points; re-open only if the backend changes.
#     No TODO for that reason: this is category (2), not a work item.
# kernel32_test.exe:profile
# UNTRIAGED, FIXABLE. 62 failures, then an unhandled 0xc0000005 — but
#     the crash is a cascade and the entry point is earlier: resource.c:680
#     and :695, GetFileMUIInfo failing 193 (ERROR_BAD_EXE_FORMAT). The bulk
#     (16 at :442 MajorVersion, 8 at :447 NumberOfIdEntries, 7 at :428
#     SizeOfRawData, 4 at :385 FindResource -> 1813, 4 at :450) is the
#     Begin/Update/EndUpdateResource path rewriting a PE on disk and then
#     reading it back. Same "write a PE, then map it" shape as
#     kernel32:actctx; triage the two together.
# TODO: Implement
# kernel32_test.exe:resource
# PROSKRNL-FLAKY BY A RACE IN THE TEST, NOT A KERNEL GAP — and its real
#     failures are FIXED. The 15 it used to have are gone: the
#     pseudo-handle range in NtWaitForMultipleObjects (10 of them) and
#     NtQueueApcThread's dead-target and NULL-routine rules (5). Both are
#     pinned in tests/ntapi (sem_wait/pseudo_handle_multi.c,
#     sem_ps/apc_dead_target.c), so they are held by the regression gate
#     whatever this pair does.
#
#     What blocks it is one assertion, sync.c:1067:
#
#         todo_wine
#         ok(n0 == 1 || broken(ret0 && n0 == 0), "Timer callback 0 ...", n0);
#
#     n0 counts a timer-queue callback for a timer created with a due time
#     of 0 and then IMMEDIATELY deleted asynchronously (:1010-:1013, the
#     delete returning ERROR_IO_PENDING). Whether the callback runs once
#     before the delete lands is a race, and the todo_wine is calibrated to
#     Wine LOSING it. proskrnl wins it on some hosts and loses it on others
#     — measured: it loses on a KVM developer box and WINS on CI — and a
#     todo_wine that passes is a failure ("Test succeeded inside todo
#     block"), so the pair's verdict flips with scheduling.
#
#     Winning the race is the WINDOWS answer (that is why the ok() expects
#     1 and why the todo is on it at all), so there is nothing to fix here:
#     the kernel cannot make the race deterministic, and making it lose on
#     purpose would be tuning to an oracle annotation instead of to a
#     contract.
#
#     No `TODO: Implement` for the same reason ntdll:time has none:
#     re-activating this needs a decision about a named flaky assertion,
#     not kernel work.
# kernel32_test.exe:sync
kernel32_test.exe:thread
kernel32_test.exe:time
kernel32_test.exe:timer
# (c) toolhelp belongs to the GUI image, not this one, and it is a whole-pair
#     dependency rather than a few assertions. Every one of its snapshot
#     tests is taken against a SUB-PROCESS it launches, and that
#     sub-process's init() does
#       `if (!LoadLibraryA("shell32.dll")) ExitProcess(WAIT_ABANDONED);`
#     (third_party/wine dlls/kernel32/tests/toolhelp.c) before it signals
#     the parent. shell32's import table names user32.dll and gdi32.dll, so
#     honouring it means putting the whole GUI stack on the CUI volume —
#     which is the boundary G7 exists to keep. Measured on proskrnl: the
#     child dies there, the parent's wait times out at toolhelp.c:649, and
#     the remaining 134 failures are all "couldn't find sub-process ..."
#     downstream of that one line. Nothing in the run convicts a kernel
#     surface: the process and thread enumeration the pair is nominally
#     about answers correctly for the three processes that DO exist.
#     Re-check by staging shell32 + user32 + gdi32 + shlwapi; if the pair
#     runs, it moves to manifest-gui.txt rather than back here.
# kernel32_test.exe:toolhelp
# (b) HOST-LOCAL ORACLE FAILURE — and NOT a kernel gap, which is why this
#     block carries no `TODO: Implement`. proskrnl's half PASSES; the ORACLE
#     half fails on at least one KVM developer box with 36 failures, 9 each
#     at version.c:1087, :1090, :1093 and :1094. One cause: the subtest
#     writes a temporary EXE and manifest and then CreateProcessA's it, the
#     spawn fails ERROR_FILE_NOT_FOUND, and the three lines after :1087 are
#     the assertions downstream of the spawn it never made.
#     Verified pre-existing on that box by checking out an earlier commit and
#     re-running the same binary under the same prefix: identical 36. The
#     runner's non-root + UTF-8 fixes are applied and do not clear it.
#
#     PARKED FOR HYGIENE, AND THE TRADE IS REAL. It is GREEN on CI, on both
#     legs, so commenting it out costs genuine CI coverage of a pair that
#     passes there. It is parked anyway because the alternative is worse: a
#     `make fulltest` that is permanently red on a developer box teaches the
#     next person to skim past the one line that matters, which is the same
#     argument the header makes for never activating a red pair.
#     Re-activating it needs the HOST question answered (why CreateProcessA
#     cannot find a file the subtest just wrote, under the pinned Wine, on
#     that box), not kernel work — so whoever fixes their environment should
#     un-comment it rather than wait for a milestone.
# kernel32_test.exe:version
# RE-MEASURED after the SEC_* modifier flags landed: 25 -> 1, and the ONE
#     that is left is the todo floor below. Every failing assertion this pair
#     had that is real work is gone; the verdict line still reads
#     FAIL (timeout) because the wedge is untouched and belongs to another
#     item, so the pair stays parked. Do not read "1 failure" as "one item
#     away from green" — see WHERE IT STOPS.
#
#     WHAT WENT, AND IT WAS ONE CAUSE FOR ALL 24. server/mapping.c
#     get_mapping_flags is twenty lines that decide three things at once, and
#     proskrnl implemented the first (which section KIND) and neither of the
#     other two. Now transcribed whole as MipMappingFlags
#     (kernel/mm/section.c) with the view half in MiCreateMappedVad
#     (kernel/mm/virtual.c); pinned by tests/ntapi/sem_mm/section_sec_flags.c,
#     docs/03 "The SEC_* modifier flags on a section". The three rules:
#
#       - WHICH COMBINATIONS REFUSE (was :1616 x5 + :1617 x5, indices
#         2/20/24/40/41). SEC_LARGE_PAGES and SEC_WRITECOMBINE are neither
#         uniformly legal nor uniformly illegal: SEC_LARGE_PAGES refuses on an
#         image, on any file-backed section and on an anonymous SEC_RESERVE —
#         and SUCCEEDS on an anonymous SEC_COMMIT, the one arm that returns
#         above the guard. No row of this matrix asks that case.
#       - WHICH BITS SURVIVE, per arm (was :1638 x13 + :1014). An anonymous
#         section keeps the caller's word WHOLE, a file-backed one keeps
#         SEC_NOCACHE|SEC_WRITECOMBINE and reports SEC_FILE for the kind, an
#         image keeps neither — so SEC_IMAGE|SEC_NOCACHE is accepted and the
#         bit dropped.
#       - WHERE THE CHECK SITS: above create_mapping's get_file_obj, so a
#         refused combination is reported for a junk file handle. No winetest
#         assertion reaches it; the pin does.
#
#     AND ONE THING BEHIND A GATE, which is docs/21 §4 trap 2 with the good
#     outcome. virtual.c:1017 is `if (section_info.Attributes & SEC_NOCACHE)`,
#     guarding 17 assertions about what a view of a SEC_NOCACHE section
#     reports. The gate reads exactly the value rule 2 was getting wrong, so
#     that block had NEVER RUN on proskrnl. It runs now and every one of the
#     17 passes.
#
#     THE WEDGE IS GONE AND THE PAIR IS MEASURED PAST IT. It stopped at
#     virtual.c:1465, one function short of test_write_watch, whose pipe leg
#     issues an overlapped ReadFile on an empty named pipe and satisfies it
#     from the SAME thread (test body :191 and :202). npfs data reads now pend
#     (docs/21 W4c, docs/03 "CUI-8 async notes", pinned
#     tests/ntapi/sem_pipe/pended_read.c), so test_write_watch runs and the
#     pair goes on through test_shared_memory to the END of the module.
#     The verdict does not go green and does not even keep its shape: it was
#     FAIL (timeout), it is now FAIL (exit=0xc0000005) after ~10 s. 1 -> 4 is
#     the trap-2 arithmetic again, not a regression — the 1 was everything a
#     stopped pair had ever executed.
#
#     WHAT IS LEFT, summing to 4:
#       -  1  :1465, THE FLOOR, unchanged: a todo_wine that proskrnl PASSES
#         (NtAreMappedFilesTheSame(kernel32.dll, a mapped view) must answer
#         STATUS_SUCCESS, proskrnl does, the oracle does not) and winetest
#         scores a succeeded todo as a FAILURE. This pair cannot reach zero
#         while proskrnl is the more NT-correct of the two runners, exactly as
#         kernel32:volume:186 cannot. Easy to miss when scanning for "Test
#         failed": it prints as "Test succeeded inside todo block".
#       -  3  :4549 DuplicateHandle -> 6 (ERROR_INVALID_HANDLE) and
#         :4571/:4572 the ReadProcessMemory behind the handle it never got —
#         test_far_regions' cross-process leg. ONE cause, and the third is
#         plain fallout of the first.
#       - then an unhandled 0xc0000005 in ntdll (RIP inside the PE, err=0x6, a
#         WRITE fault on the user stack) after :4735's single-processor skip.
#         Read it as a cascade until proved otherwise (docs/21 §4 trap 3): the
#         DuplicateHandle failure is upstream of it and is the first thing to
#         fix. And everything the fault cut off is unmeasured, so 4 is a lower
#         bound like every count this block has carried.
#
#     The measurement above used the 300 s per-pair timeout in the line below.
#     The oracle leg is green: 30936 tests executed, 140 todo, 0 failures.
# TODO: Implement
# kernel32_test.exe:virtual
# RE-TRIAGED. FOUR failures, not the three this block used to claim, and
#     only TWO of the four are kernel work. Re-measured from scratch; the
#     mount-manager half stays fixed (down from 53).
#
#     THE FOURTH, AND IT IS A FLOOR: volume.c:186 is a todo_wine that
#     proskrnl PASSES. After DefineDosDevice(DDD_REMOVE_DEFINITION), opening
#     "x:\system32" must fail ERROR_PATH_NOT_FOUND; proskrnl returns exactly
#     that (3), the oracle returns 2, and winetest scores a succeeded todo as
#     a failure. So the pair CANNOT go green while proskrnl is the more
#     NT-correct of the two runners, and making it lose would be tuning to an
#     oracle annotation instead of to a contract — the same shape as
#     kernel32:sync and kernel32:profile:229. Easy to miss when scanning a
#     log for "Test failed": it prints as "Test succeeded inside todo block".
#
#     volume.c:2022 IS NOT AN IMPLEMENTATION ITEM EITHER — it is a decision.
#     It wants FILE_SUPPORTS_OPEN_BY_FILE_ID on the volume, and the ORACLE
#     ITSELF does not report that flag for FAT or FAT32: dlls/ntdll/unix/
#     file.c sets FILE_CASE_PRESERVED_NAMES for MOUNTMGR_FS_TYPE_FAT32 and
#     reserves the flag for its NTFS default arm. The oracle leg passes only
#     because Wine's C: is a Linux directory, which takes that default arm.
#     NT on a real FAT32 volume has no file-id index and answers the way
#     proskrnl does. proskrnl's FAT32 COULD serve it — the id is already
#     stable, (dirCluster << 32) | sfnSlot, fs/fat32/fat.h FatFileId, and
#     resolution would be a tree scan — but reporting the flag on a volume
#     that names itself FAT32 claims a combination NT never shows.
#     So docs/21 W7's "larger half" needs a human decision about the FAT32
#     backend, not an implementation. ntdll:file:467 inherits it.
#
#     WHAT IS ACTUALLY KERNEL WORK is volume.c:632 and :675, and both are
#     bigger than "3 failures" suggests, because each is a win_skip standing
#     in front of a whole test function (docs/21 §4 trap 2 in a second form:
#     an early-out hides its body, not just its tail).
#       - :632, CreateFile("\\.\c:") -> error 5. Diagnosed: proskrnl resolves
#         \??\C: to the volume's ROOT DIRECTORY, so kernelbase's implied
#         FILE_NON_DIRECTORY_FILE makes it STATUS_FILE_IS_A_DIRECTORY, which
#         maps to ERROR_ACCESS_DENIED. On NT \??\C: is the VOLUME DEVICE and
#         \??\C:\ is the root directory. BLOCKED ON A NAME-PARSER CHANGE:
#         kernel/ob/namespace.c hands the FS an EMPTY remainder for both
#         spellings (the device-terminating '\' is consumed as the separator,
#         :349-:371), so the FS cannot tell them apart and the distinction
#         has to be preserved in Ob's one parse — the same engine W3 pinned.
#         Opening it exposes test_disk_extents (~6 assertions,
#         IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS).
#       - :675, CreateFile("\\.\PhysicalDrive0") -> error 2. There is no such
#         name; it needs a disk device object (\Device\Harddisk0\DR0 plus the
#         \??\PhysicalDrive0 link — both NT-present, so not a G2 addition).
#         No parser change: a disk has no filesystem, so an empty path on it
#         is unambiguous. Opening it exposes test_disk_query_property (~25
#         assertions: IOCTL_STORAGE_QUERY_PROPERTY for StorageDeviceProperty,
#         StorageDeviceSeekPenaltyProperty and StorageDeviceTrimProperty, each
#         with its STORAGE_DESCRIPTOR_HEADER truncation protocol).
#         PIN HAZARD, measured: \??\PhysicalDrive0 DOES exist on the pinned
#         oracle on this box (probed), but it exists because wineserver's
#         mountmgr enumerated the HOST's disks. A tests/ntapi pin for it would
#         put host disk topology into the permanent gate, which is the class
#         of host-dependence that already cost this project eight pairs (see
#         the header). Decide that before writing the pin.
# TODO: Implement
# kernel32_test.exe:volume

# --- msvcrt (third_party/wine/dlls/msvcrt/tests) ----------------------------
msvcrt_test.exe:cpp
msvcrt_test.exe:data
msvcrt_test.exe:dir
msvcrt_test.exe:environ
msvcrt_test.exe:file
msvcrt_test.exe:heap
msvcrt_test.exe:locale
msvcrt_test.exe:misc
msvcrt_test.exe:printf
msvcrt_test.exe:scanf
msvcrt_test.exe:signal
msvcrt_test.exe:string
msvcrt_test.exe:time

# --- ucrtbase (third_party/wine/dlls/ucrtbase/tests) ------------------------
ucrtbase_test.exe:cpp
ucrtbase_test.exe:environ
ucrtbase_test.exe:file
ucrtbase_test.exe:misc
ucrtbase_test.exe:printf
ucrtbase_test.exe:scanf
ucrtbase_test.exe:string
ucrtbase_test.exe:thread

# --- cmd (third_party/wine/programs/cmd/tests) ------------------------------
# (b) One residual oracle failure after the host fixes above took it from 3
#     to 1: batch.c:390 reads 'Failure' where the expected output says
#     'Success' at line 2301 of the suite's own transcript — the pinned
#     Wine's cmd.exe against the pinned Wine's expected file, with proskrnl
#     nowhere in it.
# cmd.exe_test.exe:batch
cmd.exe_test.exe:directory
